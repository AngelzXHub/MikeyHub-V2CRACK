local u1 = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local ok, result = pcall(function()
	return loadstring(game:HttpGet("https://raw.githubusercontent.com/RobloxScriptHub/MikeyHub-V2/main/Loader/MHExtras/MikeyHubData"))()
end)

if not ok or (result == nil or result == "") then
	game.Players.LocalPlayer:Kick("Failed to load MikeyHub Info.\nThis is either because the MikeyHub URL is invalid\nOr you're attempting to spoof it.")
end

local PlaceId = game.PlaceId
local ProductInfo = game:GetService("MarketplaceService"):GetProductInfo(PlaceId)
local ClientId = game:GetService("RbxAnalyticsService"):GetClientId()
local LocalPlayer = game:GetService("Players").LocalPlayer

local function u8()
	-- upvalues: LocalPlayer (ref)
	if game:GetService("MarketplaceService"):UserOwnsGamePassAsync(LocalPlayer.UserId, 267790155) ~= true and (not getgenv().CollectMikeyHubData or getgenv().CollectMikeyHubData.Premium ~= true) then
		return false
	end

	return true
end

local t1 = {}
local v10 = setmetatable({}, {
	__index = function(_, p2)
		-- upvalues: u8 (ref), LocalPlayer (ref), t1 (ref)
		if p2 ~= "CheckIfMikeyHubOwned" then
			return rawget(t1, p2)
		end

		return function(...)
			-- upvalues: u8 (ref), LocalPlayer (ref)
			local v294 = u8(...)

			if v294 ~= true and v294 ~= false then
				LocalPlayer:Kick("Invaild mikeyhub result")
			end

			return v294
		end
	end,
	__newindex = function(_, _, _)
		-- upvalues: LocalPlayer (ref)
		LocalPlayer:Kick("[ MikeyHub AntiCheat ] : Detected tampering with mikeyhub function nerd")
	end
})

t1.CheckIfMikeyHubOwned = u8

local u11 = v10.CheckIfMikeyHubOwned()
local s1 = ""
local v13

if not u11 then
	v13 = "MikeyHub " .. tostring(result.Version)
else
	v13 = "MikeyHub+ " .. tostring(result.Version)
end

local v14 = (function(p6)
	return (p6:gsub("%b[]", ""):gsub("%b{}", ""):gsub("%b()", ""):match("^%s*(.-)%s*$"))
end)((tostring(ProductInfo.Name)))
local u15 = u1:CreateWindow({
	Title = v13,
	SubTitle = "- By " .. tostring(result.Owner) .. " - " .. v14,
	TabWidth = 160,
	Size = UDim2.fromOffset(580, 460),
	Acrylic = getgenv().Blur or false,
	Theme = getgenv().Theme or "Dark",
	MinimizeKey = getgenv().MinizeKey or Enum.KeyCode.LeftAlt
})
local v16 = getgenv().CollectMikeyHubData and getgenv().CollectMikeyHubData.Test
local v17 = getgenv().LoaderKey and (function(p7)
	local n1 = 0

	for i = 1, #p7 do
		n1 = (n1 + string.byte(p7, i) * i) % 4294967296
	end

	return tostring(n1)
end)(getgenv().LoaderKey)

if getgenv().LoaderKey ~= nil then
	if v17 == v16 then
		local UserInputService = game:GetService("UserInputService")

		table.find({
			Enum.Platform.IOS,
			Enum.Platform.Android
		}, UserInputService:GetPlatform())

		local v19 = identifyexecutor()
		local LocalPlayer2 = game:GetService("Players").LocalPlayer
		local UserId = LocalPlayer2.UserId

		if (function()
			-- upvalues: LocalPlayer (ref), ClientId (ref), s1 (ref)
			local v184 = loadstring(game:HttpGet("https://raw.githubusercontent.com/RobloxScriptHub/MikeyHubInfo/main/BlacklistedPeople/BannedHWIDs/Main"))()
			local LocalPlayerName = LocalPlayer.Name

			for k, v in pairs(v184) do
				if k == LocalPlayerName or v.PlayerHWID == ClientId then
					s1 = tostring(v.BanReason)

					return true
				end
			end

			return false
		end)() ~= false then
			local t2 = {
				["Content-Type"] = "application/json"
			}
			local t3 = {
				embeds = {{
					author = {
						name = "Blacklisted User Has Executed! - MikeyHub",
						icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
					},
					description = "User info logged and kicked successfully",
					type = "rich",
					color = tonumber(16777215),
					fields = {
						{
							name = "HWID :",
							value = game:GetService("RbxAnalyticsService"):GetClientId(),
							inline = true
						},
						{
							name = "Player Username :",
							value = game:GetService("Players").LocalPlayer.Name,
							inline = true
						},
						{
							name = "Player DisplayName :",
							value = game:GetService("Players").LocalPlayer.DisplayName,
							inline = true
						},
						{
							name = "Player UserId :",
							value = game:GetService("Players").LocalPlayer.UserId,
							inline = true
						},
						{
							name = "Player Account Age :",
							value = game:GetService("Players").LocalPlayer.AccountAge,
							inline = true
						},
						{
							name = "Executor :",
							value = v19,
							inline = true
						},
						{
							name = "Device :",
							value = (function()
								-- upvalues: UserInputService (ref)
								local Platform = UserInputService:GetPlatform()

								if Platform ~= Enum.Platform.Android then
									if Platform ~= Enum.Platform.IOS then
										return "PC"
									end

									return "IPhone"
								end

								return "Android"
							end)(),
							inline = true
						},
						{
							name = "PandaAuth HWID :",
							value = (function()
								local v235 = cloneref(game:GetService("Players"))

								return tostring(v235.LocalPlayer.UserId)
							end)(),
							inline = true
						}
					}
				}}
			}
			local json = game:GetService("HttpService"):JSONEncode(t3);

			(http_request or (request or (HttpPost or syn.request)))({
				Url = "https://discord.com/api/webhooks/1274850660550049844/xyyZynAaVyeELiM0blJRu1pJBjwnwMi_P6a9tk_FicTQ_PO85rn_h5z_JX6aRAX_9hxr",
				Body = json,
				Method = "POST",
				Headers = t2
			})
			LocalPlayer2:Kick("\nYou're Banned From MikeyHub \n \nReason: " .. s1 .. "\n \nIf You Believe This is A Mistake\nContact Mods Within The Server.\nHWID: " .. ClientId)
		elseif getgenv().CollectMikeyHubData.KeySystem ~= true then
			local t4 = {
				CompleteSys = u15:AddTab({
					Title = "Main",
					Icon = ""
				})
			}
			local _ = u1.Options

			t4.CompleteSys:AddSection("Main")
			t4.CompleteSys:AddParagraph({
				Title = "Please Complete Key System!",
				Content = "You must complete the key system in order to use free MikeyHub, don't be weird and support me."
			})
			t4.CompleteSys:AddButton({
				Title = "Open MikeyHub Loader",
				Description = "Opens mikeyhub loader/key system.",
				Callback = function()
					-- upvalues: u1 (ref)
					loadstring(game:HttpGet("https://raw.githubusercontent.com/RobloxScriptHub/MikeyHub-V2/main/Loader/Main"))()
					u1:Destroy()
				end
			})
			u15:SelectTab(1)
		else
			local t5 = {
				Main = u15:AddTab({
					Title = "| Main",
					Icon = "home"
				}),
				Combat = u15:AddTab({
					Title = "| Combat",
					Icon = "swords"
				}),
				GunMods = u15:AddTab({
					Title = "| Gun Mods",
					Icon = "crosshair"
				}),
				Visuals = u15:AddTab({
					Title = "| Visuals",
					Icon = "eye"
				}),
				Misc = u15:AddTab({
					Title = "| Misc",
					Icon = "circle-ellipsis"
				})
			}
			local _ = u1.Options
			local u29 = false

			spawn(function()
				-- upvalues: u29 (ref)
				task.wait(2)
				u29 = true
			end)
			t5.Main:AddSection("Give/Unlock Items")

			local t6 = {}

			if LocalPlayer2.Backpack then
				for _, v in pairs(game:GetService("ReplicatedStorage").Weapons:GetChildren()) do
					if v.Name ~= "M16A1" and v.Name ~= "Fisticuffs" and v.Name ~= "Diamond Back" and v.Name ~= "Tanto" and v.Name ~= "Karambit" and v.Name ~= "TT30" and v.Name ~= "Mk PM" and v.Name ~= "PPK" and v.Name ~= "M1911" and v.Name ~= "Model 39" and v.Name ~= "Python" and v.Name ~= "Mauser C96" and v.Name ~= "Akimbo G40" and v.Name ~= "Akimbo Deagle" and v.Name ~= "P08 Luger" and v.Name ~= "Big Iron" then
						table.insert(t6, v.Name)
					end
				end
			end

			local u33 = nil

			t5.Main:AddDropdown("", {
				Title = "Gun",
				Description = "the gun you want to spawn in with\nONLY FAL IS FREE",
				Values = t6,
				Multi = false,
				Default = 1
			}):OnChanged(function(p8)
				-- upvalues: u33 (ref)
				u33 = p8
			end)
			t5.Main:AddButton({
				Title = "Give Gun",
				Description = "Gives you the gun you selected (deploy to see effects, also saves)\nFAL IS FREE",
				Callback = function()
					-- upvalues: u11 (ref), u33 (ref), u1 (ref)
					if u11 ~= true and u33 ~= "FAL" then
						u1:Notify({
							Title = "Error.",
							Content = "You Must Own Premium In Order To Use This.",
							SubContent = "Join Our Discord To Buy MikeyHub.",
							Duration = 5
						})
					else
						game:GetService("Players").LocalPlayer:SetAttribute("Primary", u33)
					end
				end
			})

			local t7 = {}

			if LocalPlayer2.Backpack then
				for _, v in pairs(game:GetService("ReplicatedStorage").Weapons:GetChildren()) do
					if v.Name == "Mk PM" or v.Name == "PPK" or v.Name == "M1911" or v.Name == "Model 39" or v.Name == "Python" or v.Name == "Mauser C96" or v.Name == "Akimbo G40" or v.Name == "Akimbo Deagle" or v.Name == "P08 Luger" or v.Name == "Big Iron" then
						table.insert(t7, v.Name)
					end
				end
			end

			local u37 = nil

			t5.Main:AddDropdown("", {
				Title = "Secondary Gun",
				Description = "the gun you want to spawn in with",
				Values = t7,
				Multi = false,
				Default = 1
			}):OnChanged(function(p9)
				-- upvalues: u37 (ref)
				u37 = p9
			end)
			t5.Main:AddButton({
				Title = "Give Secondary Gun",
				Description = "Gives you the gun you selected (deploy to see effects, also saves)\nOnly Python And Mauser Is Premium",
				Callback = function()
					-- upvalues: u37 (ref), u1 (ref)
					if u37 == "Python" and u37 == "Mauser C96" then
						u1:Notify({
							Title = "Error.",
							Content = "You Must Own Premium In Order To Use This (Other Secondarys Are Free).",
							SubContent = "Join Our Discord To Buy MikeyHub.",
							Duration = 5
						})
					else
						game:GetService("Players").LocalPlayer:SetAttribute("Secondary", u37)
					end
				end
			})

			local t8 = {}

			if LocalPlayer2.Backpack then
				for _, v in pairs(game:GetService("ReplicatedStorage").Weapons:GetChildren()) do
					if v.Name == "Combat Knife" or v.Name == "Diamond Back" or v.Name == "Tanto" or v.Name == "Karambit" then
						table.insert(t8, v.Name)
					end
				end
			end

			local u41 = nil

			t5.Main:AddDropdown("", {
				Title = "Melee",
				Description = "the melee you want to spawn in with",
				Values = t8,
				Multi = false,
				Default = 1
			}):OnChanged(function(p10)
				-- upvalues: u41 (ref)
				u41 = p10
			end)
			t5.Main:AddButton({
				Title = "Give Melee",
				Description = "Gives you the melee you selected (deploy to see effects, also saves)\nAll Melees Are Free",
				Callback = function()
					-- upvalues: u41 (ref)
					game:GetService("Players").LocalPlayer:SetAttribute("Melee", u41)
				end
			})
			t5.Main:AddSection("Player")

			local connection = nil

			infJumpDebounce = false
			t5.Main:AddToggle("", {
				Title = "Infinite Jump",
				Description = "jump jump jump",
				Default = false,
				Callback = function(p11)
					-- upvalues: connection (ref), UserInputService (ref), LocalPlayer2 (ref)
					if p11 ~= true then
						if connection then
							connection:Disconnect()
						end

						infJumpDebounce = false
					else
						connection = UserInputService.JumpRequest:Connect(function()
							-- upvalues: LocalPlayer2 (ref)
							if not infJumpDebounce then
								infJumpDebounce = true
								LocalPlayer2.Character:FindFirstChildWhichIsA("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
								wait()
								infJumpDebounce = false
							end
						end)
					end
				end
			})

			local RunService = game:GetService("RunService")

			swimming = false

			local Gravity = workspace.Gravity
			local u45 = nil

			t5.Main:AddToggle("", {
				Title = "Air Swim",
				Description = "Allows you to swim in the air.",
				Default = false,
				Callback = function(p12)
					-- upvalues: LocalPlayer2 (ref), Gravity (ref), u45 (ref), RunService (ref), UserInputService (ref)
					if p12 ~= true then
						if LocalPlayer2 and LocalPlayer2.Character and LocalPlayer2.Character:FindFirstChildWhichIsA("Humanoid") then
							workspace.Gravity = Gravity
							swimming = false

							if gravReset then
								gravReset:Disconnect()
							end

							if u45 ~= nil then
								u45:Disconnect()
								u45 = nil
							end

							local Humanoid = LocalPlayer2.Character:FindFirstChildWhichIsA("Humanoid")
							local EnumItems = Enum.HumanoidStateType:GetEnumItems()

							table.remove(EnumItems, table.find(EnumItems, Enum.HumanoidStateType.None))

							for _, v in pairs(EnumItems) do
								Humanoid:SetStateEnabled(v, true)
							end
						end
					elseif not swimming and LocalPlayer2 and LocalPlayer2.Character and LocalPlayer2.Character:FindFirstChildWhichIsA("Humanoid") then
						Gravity = workspace.Gravity
						workspace.Gravity = 0

						local Humanoid = LocalPlayer2.Character:FindFirstChildWhichIsA("Humanoid")

						gravReset = Humanoid.Died:Connect(function()
							-- upvalues: Gravity (ref)
							workspace.Gravity = Gravity
							swimming = false
						end)

						local EnumItems = Enum.HumanoidStateType:GetEnumItems()

						table.remove(EnumItems, table.find(EnumItems, Enum.HumanoidStateType.None))

						for _, v in pairs(EnumItems) do
							Humanoid:SetStateEnabled(v, false)
						end

						Humanoid:ChangeState(Enum.HumanoidStateType.Swimming)
						u45 = RunService.Heartbeat:Connect(function()
							-- upvalues: LocalPlayer2 (ref), Humanoid (ref), UserInputService (ref)
							pcall(function()
								-- upvalues: LocalPlayer2 (ref), Humanoid (ref), UserInputService (ref)
								LocalPlayer2.Character.HumanoidRootPart.Velocity = (Humanoid.MoveDirection ~= Vector3.new() or UserInputService:IsKeyDown(Enum.KeyCode.Space)) and LocalPlayer2.Character.HumanoidRootPart.Velocity or Vector3.new()
							end)
						end)
						swimming = true
					end
				end
			})
			t5.Main:AddSection("Streak Changer")

			local v46 = t5.Main:AddDropdown("", {
				Title = "Streak1",
				Description = "Basically unlock all for streaks\nyou can give yourself streaks that you don't own\nReset after deploying to see effects",
				Values = {
					"Orbital Laser",
					"Radar",
					"Minigun",
					"Akimbo AA12s",
					"Hunter Drone",
					"Forcefield",
					"Sentry",
					"Launcher",
					"Nuke"
				},
				Multi = false,
				Default = 1
			})
			local v47 = t5.Main:AddDropdown("", {
				Title = "Streak2",
				Description = "Basically unlock all for streaks\nyou can give yourself streaks that you don't own\nReset after deploying to see effects",
				Values = {
					"Orbital Laser",
					"Radar",
					"Minigun",
					"Akimbo AA12s",
					"Hunter Drone",
					"Forcefield",
					"Sentry",
					"Launcher",
					"Nuke"
				},
				Multi = false,
				Default = 1
			})
			local v48 = t5.Main:AddDropdown("", {
				Title = "Streak3",
				Description = "Basically unlock all for streaks\nyou can give yourself streaks that you don't own\nReset after deploying to see effects",
				Values = {
					"Orbital Laser",
					"Radar",
					"Minigun",
					"Akimbo AA12s",
					"Hunter Drone",
					"Forcefield",
					"Sentry",
					"Launcher",
					"Nuke"
				},
				Multi = false,
				Default = 1
			})
			local v49 = t5.Main:AddDropdown("", {
				Title = "Streak4",
				Description = "Basically unlock all for streaks\nyou can give yourself streaks that you don't own\nReset after deploying to see effects",
				Values = {
					"Orbital Laser",
					"Radar",
					"Minigun",
					"Akimbo AA12s",
					"Hunter Drone",
					"Forcefield",
					"Sentry",
					"Launcher",
					"Nuke"
				},
				Multi = false,
				Default = 1
			})

			v46:OnChanged(function(p13)
				-- upvalues: u29 (ref)
				if u29 == true then
					game:GetService("Players").LocalPlayer:SetAttribute("Streak1", p13)
				end
			end)
			v47:OnChanged(function(p14)
				-- upvalues: u29 (ref)
				if u29 == true then
					game:GetService("Players").LocalPlayer:SetAttribute("Streak2", p14)
				end
			end)
			v48:OnChanged(function(p15)
				-- upvalues: u29 (ref)
				if u29 == true then
					game:GetService("Players").LocalPlayer:SetAttribute("Streak3", p15)
				end
			end)
			v49:OnChanged(function(p16)
				-- upvalues: u29 (ref)
				if u29 == true then
					game:GetService("Players").LocalPlayer:SetAttribute("Streak4", p16)
				end
			end)
			t5.GunMods:AddSection("Gun Mods")
			t5.GunMods:AddButton({
				Title = "Infinite Ammo",
				Description = "ammo me 99999 (it works but it may be inconsistent).",
				Callback = function()
					-- upvalues: u1 (ref)
					local v237 = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/Upbolt/Hydroxide/revision/ohaux.lua"))()
					local Vortex = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex
					local n2 = 2
					local t9 = {
						[1] = "Parent",
						[2] = "Name",
						[3] = "MGL",
						[4] = "Melee",
						[5] = 2,
						[6] = "Variables"
					}
					local v241 = v237.searchClosure(Vortex, "Unnamed function", n2, t9)

					if v241 ~= "?" then
						debug.setupvalue(v241, n2, 999999)
						task.wait(0.1)

						local v242 = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/Upbolt/Hydroxide/revision/ohaux.lua"))()
						local Vortex2 = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex
						local n3 = 11
						local t10 = {
							[1] = "Parent",
							[2] = "Name",
							[3] = "script",
							[5] = "Modifiers",
							[6] = "Offset",
							[7] = "Value"
						}
						local v246 = v242.searchClosure(Vortex2, "Unnamed function", n3, t10)

						if v246 ~= "?" then
							debug.setupvalue(v246, n3, 999999)

							return
						end

						return u1:Notify({
							Title = "Error.",
							Content = "Failed to edit gun stats",
							SubContent = "",
							Duration = 5
						})
					end

					return u1:Notify({
						Title = "Error.",
						Content = "Failed to edit gun stats",
						SubContent = "",
						Duration = 5
					})
				end
			})
			t5.GunMods:AddSlider("Slider", {
				Title = "FireRate",
				Description = "Change how fast your gun shoots. (might break gun if too high)",
				Default = 700,
				Min = 1,
				Max = 5000,
				Rounding = 0.1,
				Callback = function(p17)
					-- upvalues: u11 (ref), u1 (ref)
					if u11 ~= true then
						u1:Notify({
							Title = "Error.",
							Content = "You Must Own Premium In Order To Use This.",
							SubContent = "Join Our Discord To Buy MikeyHub.",
							Duration = 5
						})
					else
						local v146 = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/Upbolt/Hydroxide/revision/ohaux.lua"))()
						local Vortex = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex
						local n4 = 4
						local t11 = {
							[1] = "Animations",
							[2] = "FOV",
							[3] = "FindFirstChild",
							[4] = "Value",
							[5] = "NewSight",
							[6] = "NewSightLine"
						}
						local v150 = v146.searchClosure(Vortex, "GatherWeaponData", n4, t11)

						if v150 == "?" then
							return u1:Notify({
								Title = "Error.",
								Content = "Failed to edit gun stats",
								SubContent = "",
								Duration = 5
							})
						end

						debug.setupvalue(v150, n4, p17)
					end
				end
			})
			LocalPlayer2:GetAttribute("Primary")
			t5.GunMods:AddSection("Gun Skin")

			local v50 = t5.GunMods:AddDropdown("Dropdown", {
				Title = "Skin",
				Description = "",
				Values = {
					"Navy",
					"Wildfire",
					"Volcanic (Free)",
					"Ice",
					"Hearts",
					"Miami Tiger",
					"Damascus",
					"Gold",
					"Haptic",
					"Blackout",
					"Warrior",
					"Blossom",
					"Freedom",
					"Dark Matter"
				},
				Multi = false,
				Default = 1
			})

			local function u51(p18, p19, p20)
				-- upvalues: u1 (ref)
				local ViewModel = workspace:FindFirstChild("ViewModel")

				if not ViewModel or not ViewModel:FindFirstChild("Gun") then
					u1:Notify({
						Title = "Error.",
						Content = "You must be ingame in order to use this.",
						SubContent = "",
						Duration = 5
					})
				elseif not true then
					u1:Notify({
						Title = "Error.",
						Content = "You must have any random camo in order to use this due to anticheat detections (recommended blue)",
						SubContent = "",
						Duration = 5
					})
				else
					for _, v in ipairs(ViewModel.Gun:GetChildren()) do
						if v:FindFirstChild("Camo") then
							v.MaterialVariant = p18

							if p19 and p19 ~= "" then
								v.BrickColor = p19
							end

							if p20 and p20 ~= "" then
								v.Color = p20
							end
						end
					end
				end
			end

			local s2 = ""

			v50:OnChanged(function(p21)
				-- upvalues: s2 (ref)
				s2 = p21
			end)
			t5.GunMods:AddButton({
				Title = "Change Skin",
				Description = "Changes skin/camo to selected value in dropdown",
				Callback = function()
					-- upvalues: s2 (ref), u1 (ref), u51 (ref), LocalPlayer2 (ref), u11 (ref)
					if s2 ~= "" then
						if s2 ~= "Navy" then
							if s2 ~= "Wildfire" then
								if s2 ~= "Ice" then
									if s2 ~= "Hearts" then
										if s2 ~= "Miami Tiger" then
											if s2 ~= "Damascus" then
												if s2 ~= "Gold" then
													if s2 ~= "Haptic" then
														if s2 ~= "Blackout" then
															if s2 ~= "Blossom" then
																if s2 ~= "Warrior" then
																	if s2 ~= "Freedom" then
																		if s2 ~= "Dark Matter" then
																			if s2 == "Volcanic (Free)" then
																				LocalPlayer2:SetAttribute("PrimaryCamo", "Volcanic")
																				u1:Notify({
																					Title = "Success!",
																					Content = "Reset to see affects (if you have default camo equipped)",
																					SubContent = "",
																					Duration = 5
																				})
																			end
																		elseif u11 ~= true then
																			u1:Notify({
																				Title = "Error.",
																				Content = "You Must Own Premium In Order To Use This.",
																				SubContent = "Join Our Discord To Buy MikeyHub.",
																				Duration = 5
																			})
																		else
																			LocalPlayer2:SetAttribute("PrimaryCamo", "Dark Matter")
																			u1:Notify({
																				Title = "Success!",
																				Content = "Reset to see affects (if you have default camo equipped)",
																				SubContent = "",
																				Duration = 5
																			})
																		end
																	elseif u11 ~= true then
																		u1:Notify({
																			Title = "Error.",
																			Content = "You Must Own Premium In Order To Use This.",
																			SubContent = "Join Our Discord To Buy MikeyHub.",
																			Duration = 5
																		})
																	else
																		local Freedom = game:GetService("ReplicatedStorage").Camos.Freedom

																		u51("FreedomCamo", Freedom.BrickColor, Freedom.Color)
																		LocalPlayer2:SetAttribute("PrimaryCamo", "Freedom")
																		u1:Notify({
																			Title = "Success!",
																			Content = "Reset to see affects (if you have default camo equipped)",
																			SubContent = "",
																			Duration = 5
																		})
																	end
																elseif u11 ~= true then
																	u1:Notify({
																		Title = "Error.",
																		Content = "You Must Own Premium In Order To Use This.",
																		SubContent = "Join Our Discord To Buy MikeyHub.",
																		Duration = 5
																	})
																else
																	local Warrior = game:GetService("ReplicatedStorage").Camos.Warrior

																	u51("CAMOWarrior", Warrior.BrickColor, Warrior.Color)
																	LocalPlayer2:SetAttribute("PrimaryCamo", "Warrior")
																	u1:Notify({
																		Title = "Success!",
																		Content = "Reset to see affects (if you have default camo equipped)",
																		SubContent = "",
																		Duration = 5
																	})
																end
															elseif u11 ~= true then
																u1:Notify({
																	Title = "Error.",
																	Content = "You Must Own Premium In Order To Use This.",
																	SubContent = "Join Our Discord To Buy MikeyHub.",
																	Duration = 5
																})
															else
																local Blossom = game:GetService("ReplicatedStorage").Camos.Blossom

																u51("BlossomCamo", Blossom.BrickColor, Blossom.Color)
																LocalPlayer2:SetAttribute("PrimaryCamo", "Blossom")
																u1:Notify({
																	Title = "Success!",
																	Content = "Reset to see affects (if you have default camo equipped)",
																	SubContent = "",
																	Duration = 5
																})
															end
														elseif u11 ~= true then
															u1:Notify({
																Title = "Error.",
																Content = "You Must Own Premium In Order To Use This.",
																SubContent = "Join Our Discord To Buy MikeyHub.",
																Duration = 5
															})
														else
															local BlackoutCamo = game:GetService("MaterialService").BlackoutCamo

															u51("BlackoutCamo", BlackoutCamo.BrickColor, BlackoutCamo.Color)
															LocalPlayer2:SetAttribute("PrimaryCamo", "Blackout")
															u1:Notify({
																Title = "Success!",
																Content = "Reset to see affects (if you have default camo equipped)",
																SubContent = "",
																Duration = 5
															})
														end
													elseif u11 ~= true then
														u1:Notify({
															Title = "Error.",
															Content = "You Must Own Premium In Order To Use This.",
															SubContent = "Join Our Discord To Buy MikeyHub.",
															Duration = 5
														})
													else
														local Haptic = game:GetService("ReplicatedStorage").Camos.Haptic

														u51("HapticCamo", Haptic.BrickColor, Haptic.Color)
														LocalPlayer2:SetAttribute("PrimaryCamo", "Haptic")
														u1:Notify({
															Title = "Success!",
															Content = "Reset to see affects (if you have default camo equipped)",
															SubContent = "",
															Duration = 5
														})
													end
												elseif u11 ~= true then
													u1:Notify({
														Title = "Error.",
														Content = "You Must Own Premium In Order To Use This.",
														SubContent = "Join Our Discord To Buy MikeyHub.",
														Duration = 5
													})
												else
													local Gold = game:GetService("ReplicatedStorage").Camos.Gold

													u51("CAMOGoldMain", Gold.BrickColor, Gold.Color)
													LocalPlayer2:SetAttribute("PrimaryCamo", "Gold")
													u1:Notify({
														Title = "Success!",
														Content = "Reset to see affects (if you have default camo equipped)",
														SubContent = "",
														Duration = 5
													})
												end
											elseif u11 ~= true then
												u1:Notify({
													Title = "Error.",
													Content = "You Must Own Premium In Order To Use This.",
													SubContent = "Join Our Discord To Buy MikeyHub.",
													Duration = 5
												})
											else
												local Damascus = game:GetService("ReplicatedStorage").Camos.Damascus

												u51("DamascusCamo", Damascus.BrickColor, Damascus.Color)
												LocalPlayer2:SetAttribute("PrimaryCamo", "Damascus")
												u1:Notify({
													Title = "Success!",
													Content = "Reset to see affects (if you have default camo equipped)",
													SubContent = "",
													Duration = 5
												})
											end
										elseif u11 ~= true then
											u1:Notify({
												Title = "Error.",
												Content = "You Must Own Premium In Order To Use This.",
												SubContent = "Join Our Discord To Buy MikeyHub.",
												Duration = 5
											})
										else
											local v207 = game:GetService("ReplicatedStorage").Camos["Miami Tiger"]

											u51("MiamiTigerCamo", v207.BrickColor, v207.Color)
											LocalPlayer2:SetAttribute("PrimaryCamo", "Miami Tiger")
											u1:Notify({
												Title = "Success!",
												Content = "Reset to see affects (if you have default camo equipped)",
												SubContent = "",
												Duration = 5
											})
										end
									elseif u11 ~= true then
										u1:Notify({
											Title = "Error.",
											Content = "You Must Own Premium In Order To Use This.",
											SubContent = "Join Our Discord To Buy MikeyHub.",
											Duration = 5
										})
									else
										local Hearts = game:GetService("ReplicatedStorage").Camos.Hearts

										u51("CAMOHearts", Hearts.BrickColor, Hearts.Color)
										LocalPlayer2:SetAttribute("PrimaryCamo", "Hearts")
										u1:Notify({
											Title = "Success!",
											Content = "Reset to see affects (if you have default camo equipped)",
											SubContent = "",
											Duration = 5
										})
									end
								else
									local Ice = game:GetService("ReplicatedStorage").Camos.Ice

									u51("IceCamo", Ice.BrickColor, Ice.Color)
									LocalPlayer2:SetAttribute("PrimaryCamo", "Ice")
									u1:Notify({
										Title = "Success!",
										Content = "Reset to see affects (if you have default camo equipped)",
										SubContent = "",
										Duration = 5
									})
								end
							else
								u51("WildfireCamo")
								LocalPlayer2:SetAttribute("PrimaryCamo", "Wildfire")
								u1:Notify({
									Title = "Success!",
									Content = "Reset to see affects (if you have default camo equipped)",
									SubContent = "",
									Duration = 5
								})
							end
						else
							u51("NavyCamo")
							LocalPlayer2:SetAttribute("PrimaryCamo", "Navy")
							u1:Notify({
								Title = "Success!",
								Content = "Reset to see affects (if you have default camo equipped)",
								SubContent = "",
								Duration = 5
							})
						end
					else
						u1:Notify({
							Title = "Error.",
							Content = "You must select a skin in order to use this.",
							SubContent = "",
							Duration = 5
						})
					end
				end
			})
			t5.GunMods:AddSection("Hit Sound")

			local v53 = t5.GunMods:AddDropdown("Dropdown", {
				Title = "Sound",
				Description = "",
				Values = {
					"Default",
					"Minecraft",
					"Fortnite Shotgun"
				},
				Multi = false,
				Default = 1
			})
			local t12 = {
				ArmHit = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex.Audio.UI.ArmHit,
				HeadHit = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex.Audio.UI.HeadHit,
				LegHit = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex.Audio.UI.LegHit,
				Lower = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex.Audio.UI.LowerTorsoHit,
				Upper = game:GetService("Players").LocalPlayer.PlayerScripts.Vortex.Audio.UI.UpperTorsoHit,
				ArmHitUi = game:GetService("StarterPlayer").StarterPlayerScripts.Vortex.Audio.UI.ArmHit,
				HeadHitUi = game:GetService("StarterPlayer").StarterPlayerScripts.Vortex.Audio.UI.HeadHit,
				LegHitUi = game:GetService("StarterPlayer").StarterPlayerScripts.Vortex.Audio.UI.LegHit,
				LowerUi = game:GetService("StarterPlayer").StarterPlayerScripts.Vortex.Audio.UI.LowerTorsoHit,
				UpperUi = game:GetService("StarterPlayer").StarterPlayerScripts.Vortex.Audio.UI.UpperTorsoHit,
				ArmHitCode = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("ArmHit") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("ArmHit")),
				HeadHitCode = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("HeadHit") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("HeadHit")),
				LegHitCode = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("LegHit") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("LegHit")),
				LowerCode = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("LowerTorsoHit") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("LowerTorsoHit")),
				UpperCode = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("UpperTorsoHit") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("UpperTorsoHit"))
			}

			v53:OnChanged(function(p22)
				-- upvalues: t12 (ref)
				if p22 ~= "Default" then
					if p22 ~= "Minecraft" then
						if p22 == "Fortnite Shotgun" then
							for _, v in pairs(t12) do
								v.SoundId = "rbxassetid://4804954860"
							end
						end
					else
						for _, v in pairs(t12) do
							v.SoundId = "rbxassetid://8766809464"
						end
					end
				else
					t12.ArmHit.SoundId = "rbxassetid://6062254646"
					t12.HeadHit.SoundId = "rbxassetid://6062253602"
					t12.LegHit.SoundId = "rbxassetid://6062254394"
					t12.Lower.SoundId = "rbxassetid://6062254394"
					t12.Upper.SoundId = "rbxassetid://6062253829"
					t12.ArmHitUi.SoundId = "rbxassetid://6062254646"
					t12.HeadHitUi.SoundId = "rbxassetid://6062253602"
					t12.LegHit.SoundId = "rbxassetid://6062254394"
					t12.LowerUi.SoundId = "rbxassetid://6062254394"
					t12.UpperUi.SoundId = "rbxassetid://6062253829"
					t12.ArmHitCode.SoundId = "rbxassetid://6062254646"
					t12.HeadHitCode.SoundId = "rbxassetid://6062253602"
					t12.LegHitCode.SoundId = "rbxassetid://6062254394"
					t12.LowerCode.SoundId = "rbxassetid://6062254394"
					t12.UpperCode.SoundId = "rbxassetid://6062253829"
				end
			end)
			t5.GunMods:AddInput("", {
				Title = "Custom Sound",
				Description = "Get it from the marketplace\n(paste the numbers in url into here)",
				Default = "",
				Placeholder = "",
				Numeric = true,
				Finished = false,
				Callback = function(p23)
					-- upvalues: t12 (ref)
					for _, v in pairs(t12) do
						v.SoundId = "rbxassetid://" .. p23
					end
				end
			})
			t5.GunMods:AddSection("Kill Sound")

			local t13 = {
				KillSound = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("Kill") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("Kill"))
			}

			t5.GunMods:AddDropdown("Dropdown", {
				Title = "Sound",
				Description = "",
				Values = {
					"Default",
					"Bruh",
					"Dragon ball hit",
					"Among Us",
					"TFS",
					"UwU"
				},
				Multi = false,
				Default = 1
			}):OnChanged(function(p24)
				-- upvalues: t13 (ref)
				if p24 ~= "Default" then
					if p24 ~= "Fortnite Down" then
						if p24 ~= "Bruh" then
							if p24 ~= "Dragon ball hit" then
								if p24 ~= "Among Us" then
									if p24 ~= "TFS" then
										if p24 == "UwU" then
											for _, v in pairs(t13) do
												v.SoundId = "rbxassetid://8323804973"
											end
										end
									else
										for _, v in pairs(t13) do
											v.SoundId = "rbxassetid://6909256325"
										end
									end
								else
									for _, v in pairs(t13) do
										v.SoundId = "rbxassetid://7227567562"
									end
								end
							else
								for _, v in pairs(t13) do
									v.SoundId = "rbxassetid://1058693352"
								end
							end
						else
							for _, v in pairs(t13) do
								v.SoundId = "rbxassetid://7616380887"
							end
						end
					else
						for _, v in pairs(t13) do
							v.SoundId = "rbxassetid://17771526755"
						end
					end
				else
					t13.KillSound.SoundId = "rbxassetid://2523782146"
				end
			end)
			t5.GunMods:AddInput("", {
				Title = "Custom Sound",
				Description = "Get it from the marketplace\n(paste the numbers in url into here)",
				Default = "",
				Placeholder = "",
				Numeric = true,
				Finished = false,
				Callback = function(p25)
					-- upvalues: t13 (ref)
					for _, v in pairs(t13) do
						v.SoundId = "rbxassetid://" .. p25
					end
				end
			})

			local u56 = false

			if u56 then
				getfenv().getfenv = function()
					return setmetatable({}, {
						__index = function()
							return function()
								return true
							end
						end
					})
				end
			end

			game:GetService("HttpService")

			local Players = game:GetService("Players")
			local UserInputService2 = game:GetService("UserInputService")
			local RunService2 = game:GetService("RunService")
			local TweenService = game:GetService("TweenService")
			local t14 = {
				Aimbot = false,
				OnePressAimingMode = false,
				AimMode = "Camera",
				SilentAimMethod = "Mouse.Hit / Mouse.Target",
				SilentAimChance = 100,
				OffAfterKill = false,
				AimKey = "RMB",
				AimPartDropdownValues = {
					"Head",
					"HumanoidRootPart"
				},
				AimPart = "HumanoidRootPart",
				RandomAimPart = false,
				UseOffset = false,
				OffsetType = "Static",
				StaticOffsetIncrement = 10,
				DynamicOffsetIncrement = 10,
				AutoOffset = false,
				MaxAutoOffset = 50,
				TeamCheck = false,
				FriendCheck = false,
				WallCheck = false,
				WaterCheck = false
			}
			local LocalPlayer3 = Players.LocalPlayer
			local Mouse = LocalPlayer3:GetMouse()
			local u64 = nil
			local MouseDeltaSensitivity = UserInputService2.MouseDeltaSensitivity
			local u66 = false
			local u67 = false
			local u68 = nil
			local u69 = nil
			local connection2 = nil

			connection2 = UserInputService2:GetPropertyChangedSignal("MouseDeltaSensitivity"):Connect(function()
				-- upvalues: u64 (ref), connection2 (ref), u66 (ref), u56 (ref), t14 (ref), MouseDeltaSensitivity (ref), UserInputService2 (ref)
				if u64 then
					if not u66 or not u56 and (getfenv().mousemoverel and t14.AimMode == "Mouse" or getfenv().hookmetamethod and getfenv().newcclosure and getfenv().checkcaller and getfenv().getnamecallmethod and t14.AimMode == "Silent") then
						MouseDeltaSensitivity = UserInputService2.MouseDeltaSensitivity
					end
				else
					connection2:Disconnect()
				end
			end)

			local v71 = t5.Combat:AddSection("Aimbot")

			v71:AddToggle("AimbotToggle", {
				Title = "Aimbot Toggle",
				Description = "Turns aimbot on/off",
				Default = t14.Aimbot
			}):OnChanged(function(p26)
				-- upvalues: t14 (ref)
				t14.Aimbot = p26
			end)

			local v72 = v71:AddKeybind("AimKeybind", {
				Title = "Aimbot Key",
				Description = "Changes the key for aimbot",
				Default = t14.AimKey,
				ChangedCallback = function(p27)
					-- upvalues: t14 (ref)
					t14.AimKey = p27
				end
			})

			if v72.Value ~= "RMB" then
				t14.AimKey = Enum.KeyCode[v72.Value]
			else
				t14.AimKey = Enum.UserInputType.MouseButton2
			end

			local u73 = v71:AddDropdown("AimPartDropdown", {
				Title = "Aimbot Part",
				Description = "Changes the aimbot part",
				Values = t14.AimPartDropdownValues,
				Default = t14.AimPart,
				Callback = function(p28)
					-- upvalues: t14 (ref)
					t14.AimPart = p28
				end
			})

			task.spawn(function()
				-- upvalues: u64 (ref), t14 (ref), u73 (ref)
				while task.wait(1) and u64 do
					if t14.RandomAimPart and #t14.AimPartDropdownValues > 0 then
						u73:SetValue(t14.AimPartDropdownValues[Random.new():NextInteger(1, #t14.AimPartDropdownValues)])
					end
				end
			end)

			local function u74(p29, p30)
				-- upvalues: u66 (ref), u68 (ref), u69 (ref), UserInputService2 (ref), MouseDeltaSensitivity (ref)
				u66 = p29 and u66 or false
				u68 = p30 and u68 or nil

				if u69 then
					u69:Cancel()
					u69 = nil
				end

				UserInputService2.MouseDeltaSensitivity = MouseDeltaSensitivity
			end

			local connection3 = nil

			connection3 = UserInputService2.InputBegan:Connect(function(input)
				-- upvalues: u64 (ref), connection3 (ref), UserInputService2 (ref), t14 (ref), u66 (ref), u74 (ref), u67 (ref)
				if u64 then
					if not UserInputService2:GetFocusedTextBox() then
						if not t14.Aimbot or input.KeyCode ~= t14.AimKey and input.UserInputType ~= t14.AimKey then
							if t14.TriggerBot and (input.KeyCode == t14.TriggerKey or input.UserInputType == t14.TriggerKey) then
								if not u67 then
									u67 = true
								else
									u67 = false
								end
							end
						elseif not u66 then
							u66 = true
						else
							u74()
						end
					end
				else
					connection3:Disconnect()
				end
			end)

			local connection4 = nil

			connection4 = UserInputService2.InputEnded:Connect(function(input)
				-- upvalues: u64 (ref), connection4 (ref), UserInputService2 (ref), u66 (ref), t14 (ref), u74 (ref), u67 (ref)
				if u64 then
					if not UserInputService2:GetFocusedTextBox() then
						if not u66 or t14.OnePressAimingMode or input.KeyCode ~= t14.AimKey and input.UserInputType ~= t14.AimKey then
							if u67 and not t14.OnePressTriggeringMode and (input.KeyCode == t14.TriggerKey or input.UserInputType == t14.TriggerKey) then
								u67 = false
							end
						else
							u74()
						end
					end
				else
					connection4:Disconnect()
				end
			end)

			local function u77(p31, p32, p33)
				return typeof(p31) == "Vector3" and (typeof(p32) == "Vector3" and (typeof(p33) == "number" and (p32 - p31).Unit * p33)) or Vector3.zero
			end
			local function u78(p34)
				-- upvalues: t14 (ref), LocalPlayer3 (ref), Players (ref), u77 (ref)
				if not p34 or not p34:FindFirstChildWhichIsA("Humanoid") or not (p34:FindFirstChildWhichIsA("Humanoid").Health > 0) or p34:FindFirstChildWhichIsA("ForceField") or not t14.AimPart or not p34:FindFirstChild(t14.AimPart) or not p34:FindFirstChild(t14.AimPart):IsA("BasePart") or not LocalPlayer3.Character or not LocalPlayer3.Character:FindFirstChildWhichIsA("Humanoid") or not (LocalPlayer3.Character:FindFirstChildWhichIsA("Humanoid").Health > 0) or not LocalPlayer3.Character:FindFirstChild(t14.AimPart) or not LocalPlayer3.Character:FindFirstChild(t14.AimPart):IsA("BasePart") then
					return false
				end

				local player = Players:GetPlayerFromCharacter(p34)

				if player and player ~= LocalPlayer3 then
					local t14AimPart = p34:FindFirstChild(t14.AimPart)
					local t14AimPart2 = LocalPlayer3.Character:FindFirstChild(t14.AimPart)

					if player:GetAttribute("Team") ~= LocalPlayer3:GetAttribute("Team") then
						if t14.WallCheck then
							local v177 = u77(t14AimPart2.Position, t14AimPart.Position, (t14AimPart.Position - t14AimPart2.Position).Magnitude)
							local raycastParams = RaycastParams.new()

							raycastParams.FilterType = Enum.RaycastFilterType.Exclude
							raycastParams.FilterDescendantsInstances = { LocalPlayer3.Character }
							raycastParams.IgnoreWater = not t14.WaterCheck

							local raycastResult = workspace:Raycast(t14AimPart2.Position, v177, raycastParams)

							if not raycastResult or not raycastResult.Instance or not raycastResult.Instance:FindFirstAncestor(player.Name) then
								return false
							end
						end

						local v180 = t14.UseOffset and (t14.AutoOffset and Vector3.new(0, t14AimPart.Position.Y * t14.StaticOffsetIncrement * (t14AimPart.Position - t14AimPart2.Position).Magnitude / 1000 <= t14.MaxAutoOffset and t14AimPart.Position.Y * t14.StaticOffsetIncrement * (t14AimPart.Position - t14AimPart2.Position).Magnitude / 1000 or t14.MaxAutoOffset, 0) + p34:FindFirstChildWhichIsA("Humanoid").MoveDirection * t14.DynamicOffsetIncrement / 10 or (t14.OffsetType == "Static" and Vector3.new(0, t14AimPart.Position.Y * t14.StaticOffsetIncrement / 10, 0) or (t14.OffsetType == "Dynamic" and p34:FindFirstChildWhichIsA("Humanoid").MoveDirection * t14.DynamicOffsetIncrement / 10 or Vector3.new(0, t14AimPart.Position.Y * t14.StaticOffsetIncrement / 10, 0) + p34:FindFirstChildWhichIsA("Humanoid").MoveDirection * t14.DynamicOffsetIncrement / 10))) or Vector3.zero
						local v181 = t14.UseNoise and Vector3.new(Random.new():NextNumber(0.5, 1), Random.new():NextNumber(0.5, 1), Random.new():NextNumber(0.5, 1)) or Vector3.zero

						return true, p34, { workspace.CurrentCamera:WorldToViewportPoint(t14AimPart.Position + v180 + v181) }, t14AimPart.Position + v180 + v181, (t14AimPart.Position + v180 + v181 - t14AimPart2.Position).Magnitude, CFrame.new(t14AimPart.Position + v180 + v181) * CFrame.fromEulerAnglesYXZ(math.rad(t14AimPart.Orientation.X), math.rad(t14AimPart.Orientation.Y), math.rad(t14AimPart.Orientation.Z)), t14AimPart
					end

					return false
				end

				return false
			end

			local t15 = {}
			local t16 = {}

			local function u81(p35)
				-- upvalues: t15 (ref)
				if p35 and t15[p35] then
					t15[p35]:Disconnect()
					table.remove(t15, p35)
				end
			end
			local function u82(p36)
				-- upvalues: t16 (ref)
				if p36 and t16[p36] then
					for _, v in next, t16[p36] do
						v:Disconnect()
					end

					table.remove(t16, p36)
				end
			end
			local function u83()
				-- upvalues: t16 (ref), u82 (ref), t15 (ref), u81 (ref)
				for k, _ in next, t16 do
					u82(k)
				end

				for k, _ in next, t15 do
					u81(k)
				end
			end
			local function u84()
				-- upvalues: u74 (ref), u67 (ref), u83 (ref)
				u74()
				u67 = false
				u83()
			end
			local function u85(p37)
				-- upvalues: Players (ref)
				if typeof(p37) == "Instance" then
					Players:GetPlayerFromCharacter(p37)
				end
			end
			local function u86(p38)
				-- upvalues: t15 (ref), u81 (ref)
				if typeof(p38) == "Instance" then
					for k, v in next, t15 do
						if p38 == v.Character then
							u81(k)
						end
					end
				end
			end

			task.spawn(function()
				-- upvalues: u56 (ref), Players (ref), LocalPlayer3 (ref), u85 (ref), t16 (ref), u86 (ref)
				if not u56 and getfenv().Drawing then
					for _, v in next, Players:GetPlayers() do
						if v ~= LocalPlayer3 and v.Character then
							local Character = v.Character

							u85(Character)
							t16[v.UserId] = {
								v.CharacterAdded:Connect(u85),
								v.CharacterRemoving:Connect(u86)
							}
						end
					end
				end
			end)

			local connection5 = nil

			connection5 = LocalPlayer3.OnTeleport:Connect(function()
				-- upvalues: u56 (ref), u64 (ref), connection5 (ref)
				if not u56 and u64 and getfenv().queue_on_teleport then
					getfenv().queue_on_teleport("getfenv().loadstring(game:HttpGet(\"https://raw.githubusercontent.com/ttwizz/Open-Aimbot/master/source.lua\", true))()")
					connection5:Disconnect()
				else
					connection5:Disconnect()
				end
			end)

			local connection6 = nil

			connection6 = Players.PlayerAdded:Connect(function(player)
				-- upvalues: u56 (ref), u64 (ref), connection6 (ref), LocalPlayer3 (ref), t16 (ref), u85 (ref), u86 (ref)
				if not u56 and u64 and getfenv().Drawing then
					if player ~= LocalPlayer3 then
						t16[player.UserId] = {
							player.CharacterAdded:Connect(u85),
							player.CharacterRemoving:Connect(u86)
						}
					end
				else
					connection6:Disconnect()
				end
			end)

			local connection7 = nil

			connection7 = Players.PlayerRemoving:Connect(function(player)
				-- upvalues: u64 (ref), LocalPlayer3 (ref), u84 (ref), connection7 (ref), u82 (ref), u81 (ref)
				if not u64 then
					connection7:Disconnect()
				elseif player ~= LocalPlayer3 then
					u82(player.UserId)
					u81(player.UserId)
				else
					u64:Destroy()
					u84()
					connection7:Disconnect()
				end
			end)

			local connection8 = nil

			connection8 = RunService2.RenderStepped:Connect(function()
				-- upvalues: u64 (ref), u84 (ref), connection8 (ref), t14 (ref), u74 (ref), u67 (ref), u66 (ref), u68 (ref), u78 (ref), Players (ref), Mouse (ref), u56 (ref), UserInputService2 (ref), u69 (ref), TweenService (ref)
				if not u64.Unloaded then
					if t14.Aimbot then
						if not t14.TriggerBot then
							u67 = false
						end
					else
						u74()
					end
				else
					u64 = nil
					u84()
					connection8:Disconnect()
				end

				if u66 then
					local v248 = u68
					local huge = math.huge

					if not u78(v248) then
						if (not v248 or t14.OffAfterKill) and v248 then
							u74()
						else
							for _, v in next, Players:GetPlayers() do
								local v252, v253, v254 = u78(v.Character)

								if v252 and v254[2] then
									local Magnitude = (Vector2.new(Mouse.X, Mouse.Y) - Vector2.new(v254[1].X, v254[1].Y)).Magnitude

									if Magnitude <= huge and Magnitude <= (t14.FoVCheck and t14.FoVRadius or huge) then
										u68 = v253
										huge = Magnitude
									end
								end
							end
						end
					end

					local v256, _, v258, v259 = u78(u68)

					if not v256 then
						u74(true)
					elseif u56 or not getfenv().mousemoverel or t14.AimMode ~= "Mouse" then
						if t14.AimMode == "Camera" then
							UserInputService2.MouseDeltaSensitivity = 0

							if not t14.UseSensitivity then
								workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, v259)
							else
								u69 = TweenService:Create(workspace.CurrentCamera, TweenInfo.new(math.clamp(t14.Sensitivity, 9, 99) / 100, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
									CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, v259)
								})
								u69:Play()
							end
						end
					elseif not v258[2] then
						u74(true)
					else
						u74(true, true)

						local MouseLocation = UserInputService2:GetMouseLocation()
						local v261 = t14.UseSensitivity and t14.Sensitivity / 10 or 10

						getfenv().mousemoverel((v258[1].X - MouseLocation.X) / v261, (v258[1].Y - MouseLocation.Y) / v261)
					end
				end
			end)
			t5.Combat:AddSection("Hitbox Expander")

			local n5 = 5
			local n6 = 5
			local n7 = 1
			local u94 = nil
			local u95 = true
			local s3 = "HumanoidRootPart"

			t5.Combat:AddSlider("", {
				Title = "HumanoidRootPart Hitbox Size",
				Description = "This is only if you have hitbox expander part on \nhumanoidrootpart",
				Default = 5,
				Min = 1,
				Max = 20,
				Rounding = 0.1,
				Callback = function(p39)
					-- upvalues: n5 (ref)
					n5 = p39
				end
			})
			t5.Combat:AddSlider("", {
				Title = "Head Hitbox Size",
				Description = "This is only if you have hitbox expander part on \nhead",
				Default = 6,
				Min = 1,
				Max = 6,
				Rounding = 0.1,
				Callback = function(p40)
					-- upvalues: n6 (ref)
					n6 = p40
				end
			})
			t5.Combat:AddToggle("", {
				Title = "Hitbox Visualizer",
				Description = "",
				Default = true,
				Callback = function(p41)
					-- upvalues: u95 (ref)
					u95 = p41
				end
			})
			t5.Combat:AddDropdown("HExpDrop", {
				Title = "Hitbox Expander Part",
				Description = "Changes the hitbox expander part.",
				Values = {
					"HumanoidRootPart",
					"Head"
				},
				Default = "HumanoidRootPart",
				Callback = function(p42)
					-- upvalues: s3 (ref)
					s3 = p42
				end
			})
			t5.Combat:AddToggle("", {
				Title = "Hitbox Expander",
				Description = "Reach but for guns?",
				Default = false,
				Callback = function(p43)
					-- upvalues: u94 (ref), s3 (ref), n7 (ref), n6 (ref), n5 (ref), u95 (ref)
					u94 = p43

					local LocalPlayer4 = game:GetService("Players").LocalPlayer

					local function v133(p44, p45)
						-- upvalues: LocalPlayer4 (ref), s3 (ref), u94 (ref), n7 (ref), n6 (ref)
						for _, v in ipairs(game:GetService("Workspace"):GetDescendants()) do
							if v:FindFirstChild("HumanoidRootPart") and v.Name ~= LocalPlayer4.Name then
								local vName = game.Players:FindFirstChild(v.Name)

								if vName and vName:GetAttribute("Team") ~= LocalPlayer4:GetAttribute("Team") then
									if s3 ~= "HumanoidRootPart" then
										if s3 == "Head" then
											local Head = v:FindFirstChild("Head")

											if Head then
												if not u94 then
													Head.Size = Vector3.new(1, 2, 1)
													Head.CanCollide = false
													Head.Transparency = 0
												elseif Head.Size ~= Vector3.new(n6, n6, n6) then
													Head.Size = Vector3.new(n6, n6, n6)
													Head.Transparency = p45 and 0.5 or 1
													Head.CanCollide = false
												end
											end
										end
									else
										local HumanoidRootPart = v.HumanoidRootPart

										if HumanoidRootPart then
											if not u94 then
												HumanoidRootPart.Size = Vector3.new(1, 2, 1)
												HumanoidRootPart.BrickColor = BrickColor.new("Really black")
												HumanoidRootPart.Transparency = p45 and 0.5 or n7
												HumanoidRootPart.Material = "Neon"
											elseif HumanoidRootPart.Size ~= Vector3.new(p44, p44, p44) then
												HumanoidRootPart.Size = Vector3.new(p44, p44, p44)
												HumanoidRootPart.BrickColor = BrickColor.new("Really black")
												HumanoidRootPart.Transparency = p45 and 0.5 or 1
												HumanoidRootPart.Material = "Neon"
											end
										end
									end
								end
							end
						end
					end

					while u94 do
						while u94 do
							v133(n5, u95)
							task.wait(0.1)
						end

						v133(2, false)
						v133(n5, u95)
						task.wait(0.1)
					end
				end
			})
			t5.Visuals:AddSection("ESP")

			local color3 = Color3.fromRGB(255, 255, 255)
			local color3_2 = Color3.fromRGB(255, 255, 255)
			local n8 = 0.5
			local n9 = 0.5
			local _ = game:GetService("Players").LocalPlayer
			local u102 = t5.Visuals:AddColorpicker("Colorpicker", {
				Title = "Fill Color",
				Description = "",
				Default = Color3.fromRGB(255, 255, 255)
			})
			local u103 = t5.Visuals:AddColorpicker("Colorpicker", {
				Title = "Outline Color",
				Description = "",
				Default = Color3.fromRGB(255, 255, 255)
			})

			u102:OnChanged(function()
				-- upvalues: color3 (ref), u102 (ref)
				color3 = u102.Value
			end)
			u103:OnChanged(function()
				-- upvalues: color3_2 (ref), u103 (ref)
				color3_2 = u103.Value
			end)
			t5.Visuals:AddSlider("", {
				Title = "Outline Transparency",
				Description = "",
				Default = 0.5,
				Min = 0,
				Max = 0.9,
				Rounding = 1,
				Callback = function(p46)
					-- upvalues: n8 (ref)
					n8 = p46
				end
			})
			t5.Visuals:AddSlider("", {
				Title = "Fill Transparency",
				Description = "",
				Default = 0.5,
				Min = 0,
				Max = 0.9,
				Rounding = 1,
				Callback = function(p47)
					-- upvalues: n9 (ref)
					n9 = p47
				end
			})
			t5.Visuals:AddToggle("", {
				Title = "ESP",
				Description = "Highlights every player, allowing you to see them from afar.",
				Default = false,
				Callback = function(p48)
					-- upvalues: color3 (ref), color3_2 (ref), n9 (ref), n8 (ref), LocalPlayer2 (ref)
					getgenv().enabled = p48
					getgenv().filluseteamcolor = true
					getgenv().outlineuseteamcolor = true
					getgenv().fillcolor = color3
					getgenv().outlinecolor = color3_2
					getgenv().filltrans = n9
					getgenv().outlinetrans = n8

					local v194 = game.CoreGui:FindFirstChild("ESPHolder") or Instance.new("Folder")

					if enabled == false then
						v194:Destroy()
					end

					if v194.Name == "Folder" then
						v194.Name = "ESPHolder"
						v194.Parent = game.CoreGui
					end

					if uselocalplayer == false and v194:FindFirstChild(game.Players.LocalPlayer.Name) then
						v194:FindFirstChild(game.Players.LocalPlayer.Name):Destroy()
					end

					if getgenv().enabled == true then
						getgenv().enabled = false
						getgenv().enabled = true
					end

					while getgenv().enabled do
						task.wait(0.5)

						for _, v in ipairs(game:GetService("Workspace"):GetDescendants()) do
							if v:FindFirstChild("HumanoidRootPart") and v.Name ~= LocalPlayer2.Name then
								local vName = game.Players:FindFirstChild(v.Name)

								if vName then
									local v198 = v194:FindFirstChild(vName.Name) or Instance.new("Highlight")

									v198.Name = vName.Name

									if uselocalplayer ~= false or v198.Name ~= game.Players.LocalPlayer.Name then
										v198.Parent = v194

										local v199 = vName:GetAttribute("Team") == game.Players.LocalPlayer:GetAttribute("Team")

										if not filluseteamcolor then
											v198.FillColor = fillcolor
										else
											v198.FillColor = v199 and fillcolor or BrickColor.Red().Color
										end

										if not outlineuseteamcolor then
											v198.OutlineColor = outlinecolor
										else
											v198.OutlineColor = v199 and outlinecolor or BrickColor.Red().Color
										end

										v198.FillTransparency = filltrans
										v198.OutlineTransparency = outlinetrans
										v198.Adornee = v
										v198.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
									end
								end
							end
						end
					end
				end
			})
			t5.Visuals:AddSection("World")

			local Lighting = game:GetService("Lighting")
			local LightingAmbient = Lighting.Ambient

			t5.Visuals:AddToggle("", {
				Title = "Full Bright",
				Description = "light up light up the world",
				Default = false,
				Callback = function(p49)
					-- upvalues: Lighting (ref), LightingAmbient (ref)
					if p49 ~= true then
						Lighting.Ambient = LightingAmbient
						Lighting.ColorShift_Bottom = Color3.new()
						Lighting.ColorShift_Top = Color3.new()
					else
						Lighting.Ambient = Color3.new(1, 1, 1)
						Lighting.ColorShift_Bottom = Color3.new(1, 1, 1)
						Lighting.ColorShift_Top = Color3.new(1, 1, 1)
					end
				end
			})
			t5.Visuals:AddToggle("", {
				Title = "Night / Day",
				Description = "Changes the time to night.",
				Default = false,
				Callback = function(p50)
					-- upvalues: Lighting (ref)
					if p50 ~= true then
						Lighting.ClockTime = 14
					else
						Lighting.ClockTime = 0
					end
				end
			})
			t5.Misc:AddSection("Main")
			t5.Misc:AddButton({
				Title = "Force Reset",
				Description = "If you wanna live, don't use this lol",
				Callback = function()
					-- upvalues: LocalPlayer2 (ref)
					LocalPlayer2.Character.Humanoid.Health = 0
				end
			})
			t5.Misc:AddButton({
				Title = "Rejoin Server",
				Description = "Rejoins the game you're currently in.",
				Callback = function()
					local TeleportService = game:GetService("TeleportService")
					local LocalPlayer5 = game:GetService("Players").LocalPlayer

					TeleportService:Teleport(game.PlaceId, LocalPlayer5)
				end
			})

			local Primary = LocalPlayer2:GetAttribute("Primary")

			t5.Misc:AddSlider("Slider", {
				Title = "Gun AimIn FOV",
				Description = "Change your gun aimin/zoomin fov, could be useful for aiming\nReset to see affects.",
				Default = game:GetService("ReplicatedStorage").Weapons[Primary].FOV.Value or 50,
				Min = 1,
				Max = 1000,
				Rounding = 0.1,
				Callback = function(p51)
					-- upvalues: LocalPlayer2 (ref), u64 (ref)
					local Primary2 = LocalPlayer2:GetAttribute("Primary")

					if game:GetService("ReplicatedStorage").Weapons:FindFirstChild(Primary2) then
						local _ = game:GetService("ReplicatedStorage").Weapons[Primary2]

						if not game:GetService("ReplicatedStorage").Weapons[Primary2]:FindFirstChild("FOV") then
							u64:Notify({
								Title = "Error.",
								Content = "Couldn't find your gun, please try again.",
								SubContent = "",
								Duration = 5
							})
						else
							game:GetService("ReplicatedStorage").Weapons[Primary2]:FindFirstChild("FOV").Value = p51
						end
					end
				end
			})
			t5.Misc:AddSection("Misc Sound Changers")

			local t17 = {
				NukeSound = game:GetService("ReplicatedFirst"):FindFirstChild("HUD") and game:GetService("ReplicatedFirst").HUD.Code:FindFirstChild("NukeIncoming") or game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") and (game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("HUD") and game:GetService("Players").LocalPlayer.PlayerGui.HUD.Code:FindFirstChild("NukeIncoming"))
			}

			t5.Misc:AddInput("", {
				Title = "Nuke Sound",
				Description = "Get it from the marketplace\n(paste the numbers in url into here)",
				Default = "",
				Placeholder = "",
				Numeric = true,
				Finished = false,
				Callback = function(p52)
					-- upvalues: t17 (ref)
					for _, v in pairs(t17) do
						v.SoundId = "rbxassetid://" .. p52
					end
				end
			})

			if getgenv().NoPopups ~= false then
			end

			spawn(function()
				-- upvalues: u15 (ref), UserId (ref)
				local v188 = math.random(100, 400)

				task.wait(v188)
				u15:Dialog({
					Title = "Hello!",
					Content = "Are You Enjoying MikeyHub?",
					Buttons = {
						{
							Title = "Yes",
							Callback = function()
								-- upvalues: UserId (ref)
								local t18 = {
									["Content-Type"] = "application/json"
								}
								local t19 = {
									embeds = {{
										author = {
											name = "User Has Selected Yes!",
											icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
										},
										description = "he likes mikeyhub i suppose",
										type = "rich",
										color = tonumber(16777215),
										fields = {{
											name = "Username :",
											value = game:GetService("Players").LocalPlayer.Name,
											inline = true
										}}
									}}
								}
								local json = game:GetService("HttpService"):JSONEncode(t19);

								(http_request or (request or (HttpPost or syn.request)))({
									Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
									Body = json,
									Method = "POST",
									Headers = t18
								})
							end
						},
						{
							Title = "No",
							Callback = function()
								-- upvalues: u15 (ref), UserId (ref)
								u15:Dialog({
									Title = "Why?",
									Content = "Why aren't you enjoying MikeyHub?",
									Buttons = {
										{
											Title = "Bad Scripts.",
											Callback = function()
												-- upvalues: UserId (ref)
												local t20 = {
													["Content-Type"] = "application/json"
												}
												local t21 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Scripts Are DooDoo, Bad, Terrible.",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t21);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t20
												})
											end
										},
										{
											Title = "Lag",
											Callback = function()
												-- upvalues: UserId (ref)
												local t22 = {
													["Content-Type"] = "application/json"
												}
												local t23 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Laggy Scripts",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t23);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t22
												})
											end
										},
										{
											Title = "Little Features",
											Callback = function()
												-- upvalues: UserId (ref)
												local t24 = {
													["Content-Type"] = "application/json"
												}
												local t25 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Little Features",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t25);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t24
												})
											end
										},
										{
											Title = "Other",
											Callback = function()
												-- upvalues: UserId (ref)
												local t26 = {
													["Content-Type"] = "application/json"
												}
												local t27 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Other / Unknown Reason",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t27);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t26
												})
											end
										}
									}
								})
							end
						},
						{
							Title = "Not Sure",
							Callback = function()
							end
						}
					}
				})
			end)
			spawn(function()
				-- upvalues: u15 (ref)
				local v137 = math.random(100, 600)

				task.wait(v137)
				u15:Dialog({
					Title = "Reminder:",
					Content = "Join Our Discord If You Want To See Updates, Sneak Peeks, Giveaways, And More!",
					Buttons = {
						{
							Title = "Copy Discord Link",
							Callback = function()
								setclipboard("https://discord.gg/cMgwWVzEm6")
							end
						},
						{
							Title = "Ignore",
							Callback = function()
							end
						}
					}
				})
			end)
			u15:SelectTab(1)
		end

		return
	end

	local LocalPlayer6 = game.Players.LocalPlayer

	if LocalPlayer6 and LocalPlayer6:IsA("Player") then
		LocalPlayer6:Kick("\nKey Mismatch\n \nIf you aren't a skid trying to bypass MikeyHub, sorry to inform you that theres no direct fix for this.")
	end

	return
end

local LocalPlayer7 = game.Players.LocalPlayer

if LocalPlayer7 and LocalPlayer7:IsA("Player") then
	LocalPlayer7:Kick("Open MikeyHub Loader Before Running This.")
end
