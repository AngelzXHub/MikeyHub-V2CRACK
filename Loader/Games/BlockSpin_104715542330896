local u1 = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local ok, result = pcall(function()
	return loadstring(game:HttpGet("https://raw.githubusercontent.com/RobloxScriptHub/MikeyHub-V2/main/Loader/MHExtras/MikeyHubData"))()
end)

if not ok or (result == nil or result == "") then
	game.Players.LocalPlayer:Kick("Failed to load MikeyHub Info.\nThis is either because the MikeyHub URL is invalid\nOr you're attempting to spoof it.")
end

local PlaceId = game.PlaceId
local ProductInfo = game:GetService("MarketplaceService"):GetProductInfo(PlaceId)
local ClientId = game:GetService("RbxAnalyticsService"):GetClientId()
local LocalPlayer = game:GetService("Players").LocalPlayer

local function u8()
	-- upvalues: LocalPlayer (ref)
	if game:GetService("MarketplaceService"):UserOwnsGamePassAsync(LocalPlayer.UserId, 267790155) ~= true and (not getgenv().CollectMikeyHubData or getgenv().CollectMikeyHubData.Premium ~= true) then
		return false
	end

	return true
end

local t1 = {}
local v10 = setmetatable({}, {
	__index = function(_, p2)
		-- upvalues: u8 (ref), LocalPlayer (ref), t1 (ref)
		if p2 ~= "CheckIfMikeyHubOwned" then
			return rawget(t1, p2)
		end

		return function(...)
			-- upvalues: u8 (ref), LocalPlayer (ref)
			local v251 = u8(...)

			if v251 ~= true and v251 ~= false then
				LocalPlayer:Kick("Invaild mikeyhub result")
			end

			return v251
		end
	end,
	__newindex = function(_, _, _)
		-- upvalues: LocalPlayer (ref)
		LocalPlayer:Kick("[ MikeyHub AntiCheat ] : Detected tampering with mikeyhub function nerd")
	end
})

t1.CheckIfMikeyHubOwned = u8

local u11 = v10.CheckIfMikeyHubOwned()
local s1 = ""
local v13

if u11 ~= true then
	v13 = "MikeyHub " .. tostring(result.Version)
else
	v13 = "MikeyHub+ " .. tostring(result.Version)
end

local v14 = (function(p6)
	return (p6:gsub("%b[]", ""):gsub("%b{}", ""):gsub("%b()", ""):match("^%s*(.-)%s*$"))
end)((tostring(ProductInfo.Name)))
local u15 = u1:CreateWindow({
	Title = v13,
	SubTitle = "- By " .. tostring(result.Owner) .. " - " .. v14,
	TabWidth = 160,
	Size = UDim2.fromOffset(580, 460),
	Acrylic = getgenv().Blur or false,
	Theme = getgenv().Theme or "Dark",
	MinimizeKey = getgenv().MinizeKey or Enum.KeyCode.LeftControl
})
local UserId = LocalPlayer.UserId
local v17 = getgenv().CollectMikeyHubData and getgenv().CollectMikeyHubData.Test
local v18 = getgenv().LoaderKey and (function(p7)
	local n1 = 0

	for i = 1, #p7 do
		n1 = (n1 + string.byte(p7, i) * i) % 4294967296
	end

	return tostring(n1)
end)(getgenv().LoaderKey)

if getgenv().LoaderKey ~= nil then
	if v18 == v17 then
		local UserInputService = game:GetService("UserInputService")

		table.find({
			Enum.Platform.IOS,
			Enum.Platform.Android
		}, UserInputService:GetPlatform())

		local v20 = identifyexecutor()
		local LocalPlayer2 = game:GetService("Players").LocalPlayer
		local u22 = false

		spawn(function()
			-- upvalues: u22 (ref)
			task.wait(2)
			u22 = true
		end)

		if (function()
			-- upvalues: LocalPlayer (ref), ClientId (ref), s1 (ref)
			local v165 = loadstring(game:HttpGet("https://raw.githubusercontent.com/RobloxScriptHub/MikeyHubInfo/main/BlacklistedPeople/BannedHWIDs/Main"))()
			local LocalPlayerName = LocalPlayer.Name

			for k, v in pairs(v165) do
				if k == LocalPlayerName or v.PlayerHWID == ClientId then
					s1 = tostring(v.BanReason)

					return true
				end
			end

			return false
		end)() ~= false then
			local t2 = {
				["Content-Type"] = "application/json"
			}
			local t3 = {
				embeds = {{
					author = {
						name = "Blacklisted User Has Executed! - MikeyHub",
						icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
					},
					description = "User info logged and kicked successfully",
					type = "rich",
					color = tonumber(16777215),
					fields = {
						{
							name = "HWID :",
							value = game:GetService("RbxAnalyticsService"):GetClientId(),
							inline = true
						},
						{
							name = "Player Username :",
							value = game:GetService("Players").LocalPlayer.Name,
							inline = true
						},
						{
							name = "Player DisplayName :",
							value = game:GetService("Players").LocalPlayer.DisplayName,
							inline = true
						},
						{
							name = "Player UserId :",
							value = game:GetService("Players").LocalPlayer.UserId,
							inline = true
						},
						{
							name = "Player Account Age :",
							value = game:GetService("Players").LocalPlayer.AccountAge,
							inline = true
						},
						{
							name = "Executor :",
							value = v20,
							inline = true
						},
						{
							name = "Device :",
							value = (function()
								-- upvalues: UserInputService (ref)
								local Platform = UserInputService:GetPlatform()

								if Platform ~= Enum.Platform.Android then
									if Platform ~= Enum.Platform.IOS then
										return "PC"
									end

									return "IPhone"
								end

								return "Android"
							end)(),
							inline = true
						},
						{
							name = "PandaAuth HWID :",
							value = (function()
								local v157 = cloneref(game:GetService("Players"))

								return tostring(v157.LocalPlayer.UserId)
							end)(),
							inline = true
						}
					}
				}}
			}
			local json = game:GetService("HttpService"):JSONEncode(t3);

			(http_request or (request or (HttpPost or syn.request)))({
				Url = "https://discord.com/api/webhooks/1274850660550049844/xyyZynAaVyeELiM0blJRu1pJBjwnwMi_P6a9tk_FicTQ_PO85rn_h5z_JX6aRAX_9hxr",
				Body = json,
				Method = "POST",
				Headers = t2
			})
			LocalPlayer2:Kick("\nYou're Banned From MikeyHub \n \nReason: " .. s1 .. "\n \nIf You Believe This is A Mistake\nContact Mods Within The Server.\nHWID: " .. ClientId)
		elseif getgenv().CollectMikeyHubData.KeySystem ~= true then
			local t4 = {
				CompleteSys = u15:AddTab({
					Title = "Main",
					Icon = ""
				})
			}
			local _ = u1.Options

			t4.CompleteSys:AddSection("Main")
			t4.CompleteSys:AddParagraph({
				Title = "Please Complete Key System!",
				Content = "You must complete the key system in order to use free MikeyHub, don't be weird and support me."
			})
			t4.CompleteSys:AddButton({
				Title = "Open MikeyHub Loader",
				Description = "Opens mikeyhub loader/key system.",
				Callback = function()
					-- upvalues: u1 (ref)
					loadstring(game:HttpGet("https://raw.githubusercontent.com/RobloxScriptHub/MikeyHub-V2/main/Loader/Main"))()
					u1:Destroy()
				end
			})
			u15:SelectTab(1)
		else
			local t5 = {
				Main = u15:AddTab({
					Title = "| Player",
					Icon = "user"
				}),
				Combat = u15:AddTab({
					Title = "| Combat",
					Icon = "swords"
				}),
				GunMods = u15:AddTab({
					Title = "| Gun Mods",
					Icon = "crosshair"
				}),
				VehicleMods = u15:AddTab({
					Title = "| Vehicle Mods",
					Icon = "car"
				}),
				Visuals = u15:AddTab({
					Title = "| Visuals",
					Icon = "eye"
				}),
				Misc = u15:AddTab({
					Title = "| Misc",
					Icon = "circle-ellipsis"
				})
			}
			local _ = u1.Options

			t5.Main:AddSection("Character")
			t5.Main:AddToggle("", {
				Title = "Infinite Stamina",
				Description = "Stamina is infinite",
				Default = false,
				Callback = function(p8)
					-- upvalues: LocalPlayer2 (ref)
					if not p8 then
						LocalPlayer2:SetAttribute("StaminaConsumeMultiplier", nil)
					else
						LocalPlayer2:SetAttribute("StaminaConsumeMultiplier", 0)
					end
				end
			})
			t5.Main:AddButton({
				Title = "No Slowdowns",
				Description = "Disables slowdowns, like being slowed from punching",
				Callback = function()
					for _, v in next, getgc(true) do
						if typeof(v) == "function" and debug.info(v, "n") == "set_walk_speed" then
							hookfunction(v, function()
								local v252 = nil

								return v252
							end)
						end
					end
				end
			})
			t5.Main:AddSection("Melee")
			t5.Main:AddToggle("", {
				Title = "Fast Melee Speed",
				Description = "Makes your melee OPP",
				Default = false,
				Callback = function(p9)
					-- upvalues: u11 (ref), LocalPlayer2 (ref), u1 (ref), u22 (ref), u15 (ref)
					if not u11 then
						if u22 then
							u15:Dialog({
								Title = "Buy MikeyHub.",
								Content = "You must own MikeyHub in order to use this.\nJoin Our Discord To Buy MikeyHub Or Press Copy Link For Gamepass Link.",
								Buttons = {
									{
										Title = "Copy Link",
										Callback = function()
											-- upvalues: u1 (ref)
											setclipboard("https://mikeyhub.sellsn.io/product/b1a3ee62-933b-4537-b612-d5525a6d31a8")
											u1:Notify({
												Title = "Copied Gamepass Url To Clipboard.",
												Content = "Paste the url into your browser.",
												SubContent = "Join Our Discord For Customer Role",
												Duration = 5
											})
										end
									},
									{
										Title = "Ok",
										Callback = function()
										end
									}
								}
							})
						end
					elseif not p9 then
						local Fists = LocalPlayer2.Character:FindFirstChild("Fists")

						if not Fists or not Fists:IsA("Tool") then
							u1:Notify({
								Title = "Error.",
								Content = "You must have fist equipped in order to use this.",
								SubContent = "",
								Duration = 5
							})
						else
							Fists:SetAttribute("Speed", 1)
						end
					else
						local Tool = LocalPlayer2.Character:FindFirstChildOfClass("Tool")

						if not Tool or not Tool:GetAttribute("Speed") then
							u1:Notify({
								Title = "Error.",
								Content = "You must have melee equipped in order to use this.",
								SubContent = "",
								Duration = 5
							})
						else
							Tool:SetAttribute("Speed", 3)
						end
					end
				end
			})
			t5.Combat:AddSection("Silent Aim")

			local Players = game:GetService("Players")
			local RunService = game:GetService("RunService")
			local UserInputService2 = game:GetService("UserInputService")
			local CurrentCamera = workspace.CurrentCamera

			workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
				-- upvalues: CurrentCamera (ref)
				CurrentCamera = workspace.CurrentCamera
			end)

			local t6 = {
				FOV = 60,
				NPCs = false,
				Players = true,
				Enabled = true,
				ShowFOV = false,
				AimTracer = true,
				DynamicFOV = true,
				FOVColor = Color3.fromRGB(255, 255, 255),
				AimTracerColor = Color3.fromRGB(255, 0, 0),
				CurrentTarget = nil
			}
			local FOV = t6.FOV
			local drawing = Drawing.new("Circle")

			drawing.NumSides = 20
			drawing.Transparency = 1
			drawing.Thickness = 2
			drawing.Color = t6.FOVColor
			drawing.Filled = false

			local drawing2 = Drawing.new("Line")

			drawing2.Thickness = 2

			local function u38()
				-- upvalues: t6 (ref), FOV (ref), CurrentCamera (ref), drawing (ref), UserInputService2 (ref)
				if not t6.ShowFOV then
					drawing.Visible = false
				else
					if not t6.DynamicFOV then
						FOV = t6.FOV
					else
						FOV = t6.FOV * (70 / CurrentCamera.FieldOfView)
					end

					drawing.Visible = true
					drawing.Radius = FOV
					drawing.Color = t6.FOVColor
					drawing.Position = UserInputService2:GetMouseLocation()
				end
			end
			local function u39()
				-- upvalues: t6 (ref), Players (ref), CurrentCamera (ref)
				local t7 = {}
				local t8 = {}

				if t6.Players then
					for _, v in ipairs(Players:GetPlayers()) do
						if v ~= Players.LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
							table.insert(t7, v.Character)
						end
					end
				end

				if t6.NPCs then
					for _, v in next, game:GetService("CollectionService"):GetTagged("NPC") do
						if v:IsDescendantOf(workspace) and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and game:GetService("CollectionService"):HasTag(v, "ActiveCharacter") then
							table.insert(t7, v)
						end
					end
				end

				for _, v in ipairs(t7) do
					local v210, v211 = CurrentCamera:WorldToViewportPoint(v.HumanoidRootPart.Position)

					if v211 then
						table.insert(t8, {
							Character = v,
							Position = Vector2.new(v210.X, v210.Y)
						})
					end
				end

				return t8
			end
			local function u40(p10)
				-- upvalues: UserInputService2 (ref)
				return (UserInputService2:GetMouseLocation() - p10).Magnitude
			end
			local function u41()
				-- upvalues: u39 (ref), u40 (ref), FOV (ref)
				local v212 = u39()
				local t9 = {}

				for _, v in ipairs(v212) do
					local v216 = u40(v.Position)

					if v216 <= FOV then
						table.insert(t9, {
							Character = v.Character,
							Distance = v216,
							Position = v.Position
						})
					end
				end

				return t9
			end
			local function u42()
				-- upvalues: u41 (ref)
				local v99 = u41()
				local Character = nil
				local huge = math.huge
				local vPosition = nil

				for _, v in ipairs(v99) do
					if huge > v.Distance then
						Character = v.Character
						vPosition = v.Position
						huge = v.Distance
					end
				end

				return Character, huge, vPosition
			end

			local connection = RunService.RenderStepped:Connect(function()
				-- upvalues: t6 (ref), u38 (ref), u42 (ref), drawing2 (ref), UserInputService2 (ref), drawing (ref)
				if not t6.Enabled then
					drawing.Visible = false
					drawing2.Visible = false
					t6.CurrentTarget = nil
				else
					u38()

					local v143, _, v145 = u42()

					t6.CurrentTarget = v143

					if not v143 then
						drawing2.Visible = false
					else
						drawing2.Visible = t6.AimTracer
						drawing2.From = UserInputService2:GetMouseLocation()
						drawing2.To = v145
						drawing2.Color = t6.AimTracerColor
					end
				end
			end)

			function t6.Unload()
				-- upvalues: connection (ref), drawing (ref), drawing2 (ref)
				connection:Disconnect()
				drawing:Remove()
				drawing2:Remove()
			end

			Aiming_Library = t6

			local Game = game:GetService("ReplicatedStorage"):WaitForChild("Modules"):WaitForChild("Game")
			local Gun = require(Game.ItemTypes:WaitForChild("Gun"))

			t5.Combat:AddToggle("", {
				Title = "Show FOV",
				Description = "shows FOV for silent aim.",
				Default = false,
				Callback = function(p11)
					Aiming_Library.ShowFOV = p11
				end
			})
			t5.Combat:AddSlider("Slider", {
				Title = "Fov Slider",
				Description = "Edits the slient aim FOV.",
				Default = 100,
				Min = 1,
				Max = 300,
				Rounding = 0.1,
				Callback = function(p12)
					Aiming_Library.FOV = p12
				end
			})

			local u46 = nil

			t5.Combat:AddToggle("", {
				Title = "Silent Aim",
				Description = "Enables/disables silent aim",
				Default = false,
				Callback = function(p13)
					-- upvalues: u11 (ref), u46 (ref), u22 (ref), u15 (ref), u1 (ref)
					if not u11 then
						if u22 then
							u15:Dialog({
								Title = "Buy MikeyHub.",
								Content = "You must own MikeyHub in order to use this.nJoin Our Discord To Buy MikeyHub Or Press Copy Link For Gamepass Link.",
								Buttons = {
									{
										Title = "Copy Link",
										Callback = function()
											-- upvalues: u1 (ref)
											setclipboard("https://mikeyhub.sellsn.io/product/b1a3ee62-933b-4537-b612-d5525a6d31a8")
											u1:Notify({
												Title = "Copied Gamepass Url To Clipboard.",
												Content = "Paste the url into your browser.",
												SubContent = "Join Our Discord For Customer Role",
												Duration = 5
											})
										end
									},
									{
										Title = "Ok",
										Callback = function()
										end
									}
								}
							})
						end
					else
						u46 = p13

						if not u46 then
							Aiming_Library.Enabled = false
						else
							Aiming_Library.Enabled = true
						end
					end
				end
			})

			local t10 = {
				Registry = {},
				GameMT = getrawmetatable(game)
			}

			function t10.RegisterHook(p14, p15, p16)
				-- upvalues: t10 (ref)
				local u126 = nil
				local u128 = "HookMgr_Hook_" .. p14
				local u129 = "HookMgr_Original_" .. p14

				shared.HookRegistry = shared.HookRegistry or {}
				shared.HookRegistry[u128] = p16

				local ok2, _ = pcall(function()
					-- upvalues: u126 (ref), p15 (ref), u128 (ref), u129 (ref)
					u126 = hookfunction(p15, loadstring("return function(...) return shared.HookRegistry[\"" .. u128 .. "\"]( shared.HookRegistry[\"" .. u129 .. "\"], ... ) end")())
				end)

				if not ok2 then
				end

				local v132 = t10.Registry[p14]

				if v132 then
					restorefunction(v132.Reference)
				end

				shared.HookRegistry[u129] = u126
				t10.Registry[p14] = {
					Original = u126 or p15,
					Reference = p15
				}

				return t10.Registry[p14]
			end
			function t10.UnregisterHook(p17)
				-- upvalues: t10 (ref)
				local u195 = t10.Registry[p17]

				if not u195 then
					print("Hook doesn't exist")
				else
					local ok3, _ = pcall(function()
						-- upvalues: u195 (ref)
						restorefunction(u195.Reference)
					end)

					if not ok3 then
						print("failed to unhook")
					end
				end

				t10.Registry[p17] = nil
			end
			function t10.ClearHooks()
				-- upvalues: t10 (ref)
				v = nil

				for _, v in next, t10.Registry do
					local ok4, _ = pcall(function()
						-- upvalues: v (ref)
						restorefunction(v.Reference)
					end)

					if not ok4 then
						print("Failed")
					end
				end

				t10.Registry = {}
			end

			t10.RegisterHook("SilentAimHook", Gun.calculate_bullet_direction, function(p18, ...)
				-- upvalues: u46 (ref)
				local CurrentTarget = Aiming_Library.CurrentTarget

				if CurrentTarget and u46 then
					local v176 = CurrentTarget:FindFirstChild("Head") or CurrentTarget:FindFirstChild("HumanoidRootPart")

					if v176 then
						return CFrame.new(workspace.CurrentCamera.CFrame.Position, v176.Position).LookVector.Unit
					end
				end

				return p18(...)
			end)

			local u48 = false

			if u48 then
				getfenv().getfenv = function()
					return setmetatable({}, {
						__index = function()
							return function()
								return true
							end
						end
					})
				end
			end

			game:GetService("HttpService")

			local Players2 = game:GetService("Players")
			local UserInputService3 = game:GetService("UserInputService")
			local RunService2 = game:GetService("RunService")
			local TweenService = game:GetService("TweenService")
			local t11 = {
				Aimbot = false,
				OnePressAimingMode = false,
				AimMode = "Camera",
				SilentAimMethod = "Mouse.Hit / Mouse.Target",
				SilentAimChance = 100,
				OffAfterKill = false,
				AimKey = "LeftAlt",
				AimPartDropdownValues = {
					"Head",
					"HumanoidRootPart"
				},
				AimPart = "HumanoidRootPart",
				RandomAimPart = false,
				UseOffset = false,
				OffsetType = "Static",
				StaticOffsetIncrement = 10,
				DynamicOffsetIncrement = 10,
				AutoOffset = false,
				MaxAutoOffset = 50,
				TeamCheck = false,
				FriendCheck = false,
				WallCheck = false,
				WaterCheck = false
			}
			local LocalPlayer3 = Players2.LocalPlayer
			local Mouse = LocalPlayer3:GetMouse()
			local MouseDeltaSensitivity = UserInputService3.MouseDeltaSensitivity
			local u57 = false
			local u58 = false
			local u59 = nil
			local u60 = nil
			local u63

			if typeof(script) ~= "Instance" or not script:FindFirstChild("Fluent") or not script:FindFirstChild("Fluent"):IsA("ModuleScript") then
				local ok5, result2 = pcall(function()
					return game:HttpGet("https://twix.cyou/Fluent.txt", true)
				end)

				if not ok5 or typeof(result2) ~= "string" or not string.find(result2, "dawid") then
					u63 = getfenv().loadstring(game:HttpGet("https://ttwizz.pages.dev/Fluent.txt", true))()
				else
					u63 = getfenv().loadstring(result2)()
				end
			else
				u63 = require(script:FindFirstChild("Fluent"))
			end

			local connection2 = nil

			connection2 = UserInputService3:GetPropertyChangedSignal("MouseDeltaSensitivity"):Connect(function()
				-- upvalues: u63 (ref), connection2 (ref), u57 (ref), u48 (ref), t11 (ref), MouseDeltaSensitivity (ref), UserInputService3 (ref)
				if u63 then
					if not u57 or not u48 and (getfenv().mousemoverel and t11.AimMode == "Mouse" or getfenv().hookmetamethod and getfenv().newcclosure and getfenv().checkcaller and getfenv().getnamecallmethod and t11.AimMode == "Silent") then
						MouseDeltaSensitivity = UserInputService3.MouseDeltaSensitivity
					end
				else
					connection2:Disconnect()
				end
			end)

			local v65 = t5.Combat:AddSection("Aimbot")

			v65:AddToggle("AimbotToggle", {
				Title = "Aimbot Toggle",
				Description = "Turns on/off Aimbot (aimbot default key is LEFTALT)",
				Default = t11.Aimbot
			}):OnChanged(function(p19)
				-- upvalues: t11 (ref)
				t11.Aimbot = p19
			end)
			v65:AddToggle("OnePressAimingModeToggle", {
				Title = "One-Press Mode",
				Description = "Instead of holding for aimbot, press button once for aimbot.",
				Default = t11.OnePressAimingMode
			}):OnChanged(function(p20)
				-- upvalues: t11 (ref)
				t11.OnePressAimingMode = p20
			end)

			local v66 = v65:AddKeybind("AimKeybind", {
				Title = "Aim Key",
				Description = "Changes the aim key for aimbot",
				Default = t11.AimKey,
				ChangedCallback = function(p21)
					-- upvalues: t11 (ref)
					t11.AimKey = p21
				end
			})

			if v66.Value ~= "RMB" then
				t11.AimKey = Enum.KeyCode[v66.Value]
			else
				t11.AimKey = Enum.UserInputType.MouseButton2
			end

			local u67 = v65:AddDropdown("AimPartDropdown", {
				Title = "Aim Part",
				Description = "Changes the aim part for aimbot",
				Values = t11.AimPartDropdownValues,
				Default = t11.AimPart,
				Callback = function(p22)
					-- upvalues: t11 (ref)
					t11.AimPart = p22
				end
			})

			task.spawn(function()
				-- upvalues: u63 (ref), t11 (ref), u67 (ref)
				while task.wait(1) and u63 do
					if t11.RandomAimPart and #t11.AimPartDropdownValues > 0 then
						u67:SetValue(t11.AimPartDropdownValues[Random.new():NextInteger(1, #t11.AimPartDropdownValues)])
					end
				end
			end)

			local function u68(p23, p24)
				-- upvalues: u57 (ref), u59 (ref), u60 (ref), UserInputService3 (ref), MouseDeltaSensitivity (ref)
				u57 = p23 and u57 or false
				u59 = p24 and u59 or nil

				if u60 then
					u60:Cancel()
					u60 = nil
				end

				UserInputService3.MouseDeltaSensitivity = MouseDeltaSensitivity
			end

			local connection3 = nil

			connection3 = UserInputService3.InputBegan:Connect(function(input)
				-- upvalues: u63 (ref), connection3 (ref), UserInputService3 (ref), t11 (ref), u57 (ref), u68 (ref), u58 (ref)
				if u63 then
					if not UserInputService3:GetFocusedTextBox() then
						if not t11.Aimbot or input.KeyCode ~= t11.AimKey and input.UserInputType ~= t11.AimKey then
							if t11.TriggerBot and (input.KeyCode == t11.TriggerKey or input.UserInputType == t11.TriggerKey) then
								if not u58 then
									u58 = true
								else
									u58 = false
								end
							end
						elseif not u57 then
							u57 = true
						else
							u68()
						end
					end
				else
					connection3:Disconnect()
				end
			end)

			local connection4 = nil

			connection4 = UserInputService3.InputEnded:Connect(function(input)
				-- upvalues: u63 (ref), connection4 (ref), UserInputService3 (ref), u57 (ref), t11 (ref), u68 (ref), u58 (ref)
				if u63 then
					if not UserInputService3:GetFocusedTextBox() then
						if not u57 or t11.OnePressAimingMode or input.KeyCode ~= t11.AimKey and input.UserInputType ~= t11.AimKey then
							if u58 and not t11.OnePressTriggeringMode and (input.KeyCode == t11.TriggerKey or input.UserInputType == t11.TriggerKey) then
								u58 = false
							end
						else
							u68()
						end
					end
				else
					connection4:Disconnect()
				end
			end)

			local function u71(p25, p26, p27)
				return typeof(p25) == "Vector3" and (typeof(p26) == "Vector3" and (typeof(p27) == "number" and (p26 - p25).Unit * p27)) or Vector3.zero
			end
			local function u72(p28)
				-- upvalues: t11 (ref), LocalPlayer3 (ref), Players2 (ref), u71 (ref)
				if not p28 or not p28:FindFirstChildWhichIsA("Humanoid") or not (p28:FindFirstChildWhichIsA("Humanoid").Health > 0) or p28:FindFirstChildWhichIsA("ForceField") or not t11.AimPart or not p28:FindFirstChild(t11.AimPart) or not p28:FindFirstChild(t11.AimPart):IsA("BasePart") or not LocalPlayer3.Character or not LocalPlayer3.Character:FindFirstChildWhichIsA("Humanoid") or not (LocalPlayer3.Character:FindFirstChildWhichIsA("Humanoid").Health > 0) or not LocalPlayer3.Character:FindFirstChild(t11.AimPart) or not LocalPlayer3.Character:FindFirstChild(t11.AimPart):IsA("BasePart") then
					return false
				end

				local player = Players2:GetPlayerFromCharacter(p28)

				if player and player ~= LocalPlayer3 then
					local t11AimPart = p28:FindFirstChild(t11.AimPart)
					local t11AimPart2 = LocalPlayer3.Character:FindFirstChild(t11.AimPart)

					if t11.WallCheck then
						local v183 = u71(t11AimPart2.Position, t11AimPart.Position, (t11AimPart.Position - t11AimPart2.Position).Magnitude)
						local raycastParams = RaycastParams.new()

						raycastParams.FilterType = Enum.RaycastFilterType.Exclude
						raycastParams.FilterDescendantsInstances = { LocalPlayer3.Character }
						raycastParams.IgnoreWater = not t11.WaterCheck

						local raycastResult = workspace:Raycast(t11AimPart2.Position, v183, raycastParams)

						if not raycastResult or not raycastResult.Instance or not raycastResult.Instance:FindFirstAncestor(player.Name) then
							return false
						end
					end

					local v186 = t11.UseOffset and (t11.AutoOffset and Vector3.new(0, t11AimPart.Position.Y * t11.StaticOffsetIncrement * (t11AimPart.Position - t11AimPart2.Position).Magnitude / 1000 <= t11.MaxAutoOffset and t11AimPart.Position.Y * t11.StaticOffsetIncrement * (t11AimPart.Position - t11AimPart2.Position).Magnitude / 1000 or t11.MaxAutoOffset, 0) + p28:FindFirstChildWhichIsA("Humanoid").MoveDirection * t11.DynamicOffsetIncrement / 10 or (t11.OffsetType == "Static" and Vector3.new(0, t11AimPart.Position.Y * t11.StaticOffsetIncrement / 10, 0) or (t11.OffsetType == "Dynamic" and p28:FindFirstChildWhichIsA("Humanoid").MoveDirection * t11.DynamicOffsetIncrement / 10 or Vector3.new(0, t11AimPart.Position.Y * t11.StaticOffsetIncrement / 10, 0) + p28:FindFirstChildWhichIsA("Humanoid").MoveDirection * t11.DynamicOffsetIncrement / 10))) or Vector3.zero
					local v187 = t11.UseNoise and Vector3.new(Random.new():NextNumber(0.5, 1), Random.new():NextNumber(0.5, 1), Random.new():NextNumber(0.5, 1)) or Vector3.zero

					return true, p28, { workspace.CurrentCamera:WorldToViewportPoint(t11AimPart.Position + v186 + v187) }, t11AimPart.Position + v186 + v187, (t11AimPart.Position + v186 + v187 - t11AimPart2.Position).Magnitude, CFrame.new(t11AimPart.Position + v186 + v187) * CFrame.fromEulerAnglesYXZ(math.rad(t11AimPart.Orientation.X), math.rad(t11AimPart.Orientation.Y), math.rad(t11AimPart.Orientation.Z)), t11AimPart
				end

				return false
			end

			local t12 = {}
			local t13 = {}

			local function u75(p29)
				-- upvalues: t12 (ref)
				if p29 and t12[p29] then
					t12[p29]:Disconnect()
					table.remove(t12, p29)
				end
			end
			local function u76(p30)
				-- upvalues: t13 (ref)
				if p30 and t13[p30] then
					for _, v in next, t13[p30] do
						v:Disconnect()
					end

					table.remove(t13, p30)
				end
			end
			local function u77()
				-- upvalues: t13 (ref), u76 (ref), t12 (ref), u75 (ref)
				for k, _ in next, t13 do
					u76(k)
				end

				for k, _ in next, t12 do
					u75(k)
				end
			end
			local function u78()
				-- upvalues: u68 (ref), u58 (ref), u77 (ref)
				u68()
				u58 = false
				u77()
			end
			local function u79(p31)
				-- upvalues: Players2 (ref)
				if typeof(p31) == "Instance" then
					Players2:GetPlayerFromCharacter(p31)
				end
			end
			local function u80(p32)
				-- upvalues: t12 (ref), u75 (ref)
				if typeof(p32) == "Instance" then
					for k, v in next, t12 do
						if p32 == v.Character then
							u75(k)
						end
					end
				end
			end

			task.spawn(function()
				-- upvalues: u48 (ref), Players2 (ref), LocalPlayer3 (ref), u79 (ref), t13 (ref), u80 (ref)
				if not u48 and getfenv().Drawing then
					for _, v in next, Players2:GetPlayers() do
						if v ~= LocalPlayer3 and v.Character then
							local Character = v.Character

							u79(Character)
							t13[v.UserId] = {
								v.CharacterAdded:Connect(u79),
								v.CharacterRemoving:Connect(u80)
							}
						end
					end
				end
			end)

			local connection5 = nil

			connection5 = LocalPlayer3.OnTeleport:Connect(function()
				-- upvalues: u48 (ref), u63 (ref), connection5 (ref)
				if not u48 and u63 and getfenv().queue_on_teleport then
					getfenv().queue_on_teleport("getfenv().loadstring(game:HttpGet(\"https://raw.githubusercontent.com/ttwizz/Open-Aimbot/master/source.lua\", true))()")
					connection5:Disconnect()
				else
					connection5:Disconnect()
				end
			end)

			local connection6 = nil

			connection6 = Players2.PlayerAdded:Connect(function(player)
				-- upvalues: u48 (ref), u63 (ref), connection6 (ref), LocalPlayer3 (ref), t13 (ref), u79 (ref), u80 (ref)
				if not u48 and u63 and getfenv().Drawing then
					if player ~= LocalPlayer3 then
						t13[player.UserId] = {
							player.CharacterAdded:Connect(u79),
							player.CharacterRemoving:Connect(u80)
						}
					end
				else
					connection6:Disconnect()
				end
			end)

			local connection7 = nil

			connection7 = Players2.PlayerRemoving:Connect(function(player)
				-- upvalues: u63 (ref), LocalPlayer3 (ref), u78 (ref), connection7 (ref), u76 (ref), u75 (ref)
				if not u63 then
					connection7:Disconnect()
				elseif player ~= LocalPlayer3 then
					u76(player.UserId)
					u75(player.UserId)
				else
					u63:Destroy()
					u78()
					connection7:Disconnect()
				end
			end)

			local connection8 = nil

			connection8 = RunService2.RenderStepped:Connect(function()
				-- upvalues: u63 (ref), u78 (ref), connection8 (ref), t11 (ref), u68 (ref), u58 (ref), u57 (ref), u59 (ref), u72 (ref), Players2 (ref), Mouse (ref), u48 (ref), UserInputService3 (ref), u60 (ref), TweenService (ref)
				if not u63.Unloaded then
					if t11.Aimbot then
						if not t11.TriggerBot then
							u58 = false
						end
					else
						u68()
					end
				else
					u63 = nil
					u78()
					connection8:Disconnect()
				end

				if u57 then
					local v230 = u59
					local huge = math.huge

					if not u72(v230) then
						if (not v230 or t11.OffAfterKill) and v230 then
							u68()
						else
							for _, v in next, Players2:GetPlayers() do
								local v234, v235, v236 = u72(v.Character)

								if v234 and v236[2] then
									local Magnitude = (Vector2.new(Mouse.X, Mouse.Y) - Vector2.new(v236[1].X, v236[1].Y)).Magnitude

									if Magnitude <= huge and Magnitude <= (t11.FoVCheck and t11.FoVRadius or huge) then
										u59 = v235
										huge = Magnitude
									end
								end
							end
						end
					end

					local v238, _, v240, v241 = u72(u59)

					if not v238 then
						u68(true)
					elseif u48 or not getfenv().mousemoverel or t11.AimMode ~= "Mouse" then
						if t11.AimMode == "Camera" then
							UserInputService3.MouseDeltaSensitivity = 0

							if not t11.UseSensitivity then
								workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, v241)
							else
								u60 = TweenService:Create(workspace.CurrentCamera, TweenInfo.new(math.clamp(t11.Sensitivity, 9, 99) / 100, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {
									CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, v241)
								})
								u60:Play()
							end
						end
					elseif not v240[2] then
						u68(true)
					else
						u68(true, true)

						local MouseLocation = UserInputService3:GetMouseLocation()
						local v243 = t11.UseSensitivity and t11.Sensitivity / 10 or 10

						getfenv().mousemoverel((v240[1].X - MouseLocation.X) / v243, (v240[1].Y - MouseLocation.Y) / v243)
					end
				end
			end)
			t5.GunMods:AddSection("Main")
			t5.GunMods:AddButton({
				Title = "Fast FireRate",
				Description = "gun shoots faster (might have to reequip your gun to see effects)",
				Callback = function()
					-- upvalues: u11 (ref), LocalPlayer2 (ref), u63 (ref), u22 (ref), u15 (ref)
					if not u11 then
						if u22 then
							u15:Dialog({
								Title = "Buy MikeyHub.",
								Content = "You must own MikeyHub in order to use this.\nJoin Our Discord To Buy MikeyHub Or Press Copy Link For Gamepass Link.",
								Buttons = {
									{
										Title = "Copy Link",
										Callback = function()
											-- upvalues: u63 (ref)
											setclipboard("https://mikeyhub.sellsn.io/product/b1a3ee62-933b-4537-b612-d5525a6d31a8")
											u63:Notify({
												Title = "Copied Gamepass Url To Clipboard.",
												Content = "Paste the url into your browser.",
												SubContent = "Join Our Discord For Customer Role",
												Duration = 5
											})
										end
									},
									{
										Title = "Ok",
										Callback = function()
										end
									}
								}
							})
						end
					else
						local Tool = LocalPlayer2.Character:FindFirstChildOfClass("Tool")

						if not Tool or not Tool:GetAttribute("FireRate") then
							u63:Notify({
								Title = "Error.",
								Content = "You Must Have The Gun Equipped To Use This.",
								SubContent = "",
								Duration = 5
							})
						else
							Tool:SetAttribute("FireRate", 999999)
						end
					end
				end
			})
			t5.GunMods:AddButton({
				Title = "Infinite Range",
				Description = "Sniper",
				Callback = function()
					-- upvalues: LocalPlayer2 (ref), u63 (ref)
					local Tool = LocalPlayer2.Character:FindFirstChildOfClass("Tool")

					if not Tool or not Tool:GetAttribute("Range") then
						u63:Notify({
							Title = "Error.",
							Content = "You Must Have The Gun Equipped To Use This.",
							SubContent = "",
							Duration = 5
						})
					else
						Tool:SetAttribute("Range", 9999)
					end
				end
			})
			t5.GunMods:AddButton({
				Title = "No Recoil",
				Description = "no movements when u shoot ",
				Callback = function()
					-- upvalues: LocalPlayer2 (ref), u63 (ref)
					local Tool = LocalPlayer2.Character:FindFirstChildOfClass("Tool")

					if not Tool or not Tool:GetAttribute("Recoil") then
						u63:Notify({
							Title = "Error.",
							Content = "You Must Have The Gun Equipped To Use This.",
							SubContent = "",
							Duration = 5
						})
					else
						Tool:SetAttribute("Recoil", 0)
					end
				end
			})
			t5.GunMods:AddButton({
				Title = "Instant Reload",
				Description = "",
				Callback = function()
					-- upvalues: u11 (ref), LocalPlayer2 (ref), u63 (ref), u22 (ref), u15 (ref)
					if not u11 then
						if u22 then
							u15:Dialog({
								Title = "Buy MikeyHub.",
								Content = "You must own MikeyHub in order to use this.\nJoin Our Discord To Buy MikeyHub Or Press Copy Link For Gamepass Link.",
								Buttons = {
									{
										Title = "Copy Link",
										Callback = function()
											-- upvalues: u63 (ref)
											setclipboard("https://mikeyhub.sellsn.io/product/b1a3ee62-933b-4537-b612-d5525a6d31a8")
											u63:Notify({
												Title = "Copied Gamepass Url To Clipboard.",
												Content = "Paste the url into your browser.",
												SubContent = "Join Our Discord For Customer Role",
												Duration = 5
											})
										end
									},
									{
										Title = "Ok",
										Callback = function()
										end
									}
								}
							})
						end
					else
						local Tool = LocalPlayer2.Character:FindFirstChildOfClass("Tool")

						if not Tool or not Tool:GetAttribute("ReloadTime") then
							u63:Notify({
								Title = "Error.",
								Content = "You Must Have The Gun Equipped To Use This.",
								SubContent = "",
								Duration = 5
							})
						else
							Tool:SetAttribute("ReloadTime", 0)
						end
					end
				end
			})
			t5.GunMods:AddToggle("MyToggle", {
				Title = "Full Auto",
				Description = "Makes your gun automatic (might have to requip weapon)",
				Default = false,
				Callback = function(p33)
					-- upvalues: LocalPlayer2 (ref), u63 (ref)
					local Tool = LocalPlayer2.Character:FindFirstChildOfClass("Tool")

					if not Tool or not Tool:GetAttribute("Automatic") then
						u63:Notify({
							Title = "Error.",
							Content = "You Must Have The Gun Equipped To Use This.",
							SubContent = "",
							Duration = 5
						})
					else
						Tool:SetAttribute("Automatic", p33)
					end
				end
			})

			local s2 = "None"
			local t14 = {}
			local car = game:GetService("ReplicatedStorage").Items.car

			if car then
				for _, v in ipairs(car:GetChildren()) do
					if v.Name ~= "Template" then
						table.insert(t14, v.Name)
					end
				end
			end

			t5.VehicleMods:AddDropdown("", {
				Title = "Vechicle",
				Description = "The vehicle you want to mod.",
				Values = t14,
				Multi = false,
				Default = 1
			}):OnChanged(function(p34)
				-- upvalues: s2 (ref)
				s2 = tostring(p34)
			end)
			t5.VehicleMods:AddSlider("Slider", {
				Title = "Max Speed",
				Description = "Edits your vehicle max speed",
				Default = 100,
				Min = 1,
				Max = 9000,
				Rounding = 0.1,
				Callback = function(p35)
					-- upvalues: u11 (ref), s2 (ref), u63 (ref), u22 (ref), u15 (ref)
					if not u11 then
						if u22 then
							u15:Dialog({
								Title = "Buy MikeyHub.",
								Content = "You must own MikeyHub in order to use this.\nJoin Our Discord To Buy MikeyHub Or Press Copy Link For Gamepass Link.",
								Buttons = {
									{
										Title = "Copy Link",
										Callback = function()
											-- upvalues: u63 (ref)
											setclipboard("https://mikeyhub.sellsn.io/product/b1a3ee62-933b-4537-b612-d5525a6d31a8")
											u63:Notify({
												Title = "Copied Gamepass Url To Clipboard.",
												Content = "Paste the url into your browser.",
												SubContent = "Join Our Discord For Customer Role",
												Duration = 5
											})
										end
									},
									{
										Title = "Ok",
										Callback = function()
										end
									}
								}
							})
						end
					else
						local ok6, result3 = pcall(function()
							-- upvalues: s2 (ref), p35 (ref)
							loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/Upbolt/Hydroxide/revision/ohaux.lua"))()

							for _, v in ipairs(workspace.Vehicles:GetChildren()) do
								if v.Name == s2 then
									v.Motors:SetAttribute("forwardMaxSpeed", p35)
								end
							end
						end)

						if not ok6 then
							u63:Notify({
								Title = "Error.",
								Content = result3,
								SubContent = "Must be in a vehicle.",
								Duration = 5
							})
						end
					end
				end
			})
			t5.VehicleMods:AddSlider("Slider", {
				Title = "Acceleration",
				Description = "Edits your vehicle acceleration.",
				Default = 100,
				Min = 1,
				Max = 1000,
				Rounding = 0.1,
				Callback = function(p36)
					-- upvalues: s2 (ref), u63 (ref)
					local ok7, result4 = pcall(function()
						-- upvalues: s2 (ref), p36 (ref)
						loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/Upbolt/Hydroxide/revision/ohaux.lua"))()

						for _, v in ipairs(workspace.Vehicles:GetChildren()) do
							if v.Name == s2 then
								v.Motors:SetAttribute("acceleration", p36)
							end
						end
					end)

					if not ok7 then
						u63:Notify({
							Title = "Error.",
							Content = result4,
							SubContent = "Must be in a vehicle.",
							Duration = 5
						})
					end
				end
			})
			t5.Misc:AddSection("Main")
			t5.Misc:AddButton({
				Title = "Force Reset",
				Description = "If you wanna live, don't use this lol",
				Callback = function()
					-- upvalues: LocalPlayer2 (ref)
					LocalPlayer2.Character.Humanoid.Health = 0
				end
			})
			t5.Misc:AddButton({
				Title = "Rejoin Server",
				Description = "Rejoins the game you're currently in.",
				Callback = function()
					local TeleportService = game:GetService("TeleportService")
					local LocalPlayer4 = game:GetService("Players").LocalPlayer

					TeleportService:Teleport(game.PlaceId, LocalPlayer4)
				end
			})
			t5.Visuals:AddSection("ESP")

			local color3 = Color3.fromRGB(255, 255, 255)
			local color3_2 = Color3.fromRGB(255, 255, 255)
			local u92 = true
			local u93 = t5.Visuals:AddColorpicker("Colorpicker", {
				Title = "Fill Color",
				Description = "",
				Default = Color3.fromRGB(255, 255, 255)
			})
			local u94 = t5.Visuals:AddColorpicker("Colorpicker", {
				Title = "Outline Color",
				Description = "",
				Default = Color3.fromRGB(255, 255, 255)
			})

			u93:OnChanged(function()
				-- upvalues: color3 (ref), u93 (ref)
				color3 = u93.Value
			end)
			u94:OnChanged(function()
				-- upvalues: color3_2 (ref), u94 (ref)
				color3_2 = u94.Value
			end)
			t5.Visuals:AddToggle("", {
				Title = "Use Team Color",
				Description = "Changes the color of esp to team color",
				Default = true,
				Callback = function(p37)
					-- upvalues: u92 (ref)
					u92 = p37
				end
			})
			t5.Visuals:AddToggle("", {
				Title = "ESP",
				Description = "Highlights every player, allowing you to see them from afar.",
				Default = false,
				Callback = function(p38)
					-- upvalues: u92 (ref), color3 (ref), color3_2 (ref)
					getgenv().enabled = p38
					getgenv().filluseteamcolor = u92
					getgenv().outlineuseteamcolor = u92
					getgenv().fillcolor = color3
					getgenv().outlinecolor = color3_2
					getgenv().filltrans = 0.7
					getgenv().outlinetrans = 0
					loadstring(game:HttpGet("https://raw.githubusercontent.com/zntly/highlight-esp/main/esp.lua"))()
				end
			})
			t5.Visuals:AddSection("World")

			local Lighting = game:GetService("Lighting")
			local LightingAmbient = Lighting.Ambient

			t5.Visuals:AddToggle("", {
				Title = "Full Bright",
				Description = "light up light up the world",
				Default = false,
				Callback = function(p39)
					-- upvalues: Lighting (ref), LightingAmbient (ref)
					if p39 ~= true then
						Lighting.Ambient = LightingAmbient
						Lighting.ColorShift_Bottom = Color3.new()
						Lighting.ColorShift_Top = Color3.new()
					else
						Lighting.Ambient = Color3.new(1, 1, 1)
						Lighting.ColorShift_Bottom = Color3.new(1, 1, 1)
						Lighting.ColorShift_Top = Color3.new(1, 1, 1)
					end
				end
			})

			if getgenv().NoPopups ~= false then
			end

			spawn(function()
				-- upvalues: u15 (ref), UserId (ref)
				local v190 = math.random(100, 400)

				task.wait(v190)
				u15:Dialog({
					Title = "Hello!",
					Content = "Are You Enjoying MikeyHub?",
					Buttons = {
						{
							Title = "Yes",
							Callback = function()
								-- upvalues: UserId (ref)
								local t15 = {
									["Content-Type"] = "application/json"
								}
								local t16 = {
									embeds = {{
										author = {
											name = "User Has Selected Yes!",
											icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
										},
										description = "he likes mikeyhub i suppose",
										type = "rich",
										color = tonumber(16777215),
										fields = {{
											name = "Username :",
											value = game:GetService("Players").LocalPlayer.Name,
											inline = true
										}}
									}}
								}
								local json = game:GetService("HttpService"):JSONEncode(t16);

								(http_request or (request or (HttpPost or syn.request)))({
									Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
									Body = json,
									Method = "POST",
									Headers = t15
								})
							end
						},
						{
							Title = "No",
							Callback = function()
								-- upvalues: u15 (ref), UserId (ref)
								u15:Dialog({
									Title = "Why?",
									Content = "Why aren't you enjoying MikeyHub?",
									Buttons = {
										{
											Title = "Bad Scripts.",
											Callback = function()
												-- upvalues: UserId (ref)
												local t17 = {
													["Content-Type"] = "application/json"
												}
												local t18 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Scripts Are DooDoo, Bad, Terrible.",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t18);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t17
												})
											end
										},
										{
											Title = "Lag",
											Callback = function()
												-- upvalues: UserId (ref)
												local t19 = {
													["Content-Type"] = "application/json"
												}
												local t20 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Laggy Scripts",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t20);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t19
												})
											end
										},
										{
											Title = "Little Features",
											Callback = function()
												-- upvalues: UserId (ref)
												local t21 = {
													["Content-Type"] = "application/json"
												}
												local t22 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Little Features",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t22);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t21
												})
											end
										},
										{
											Title = "Other",
											Callback = function()
												-- upvalues: UserId (ref)
												local t23 = {
													["Content-Type"] = "application/json"
												}
												local t24 = {
													embeds = {{
														author = {
															name = "User Has Selected No.",
															icon_url = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=420&height=420"
														},
														description = "he doesn't like mikeyhub i suppose",
														type = "rich",
														color = tonumber(16777215),
														fields = {
															{
																name = "Username :",
																value = game:GetService("Players").LocalPlayer.Name,
																inline = true
															},
															{
																name = "Reason :",
																value = "Other / Unknown Reason",
																inline = true
															}
														}
													}}
												}
												local json = game:GetService("HttpService"):JSONEncode(t24);

												(http_request or (request or (HttpPost or syn.request)))({
													Url = "https://discord.com/api/webhooks/1270537407263150152/J2CeU61pacElkzcffrM6_Hd2ZDfmsuPBOgyzlvCD5v5QWDP__lik6yIh00zhsYniqxJ5",
													Body = json,
													Method = "POST",
													Headers = t23
												})
											end
										}
									}
								})
							end
						},
						{
							Title = "Not Sure",
							Callback = function()
							end
						}
					}
				})
			end)
			spawn(function()
				-- upvalues: u15 (ref)
				local v189 = math.random(100, 600)

				task.wait(v189)
				u15:Dialog({
					Title = "Reminder:",
					Content = "Join Our Discord If You Want To See Updates, Sneak Peeks, Giveaways, And More!",
					Buttons = {
						{
							Title = "Copy Discord Link",
							Callback = function()
								setclipboard("https://discord.gg/cMgwWVzEm6")
							end
						},
						{
							Title = "Ignore",
							Callback = function()
							end
						}
					}
				})
			end)
			u15:SelectTab(1)
		end

		return
	end

	local LocalPlayer5 = game.Players.LocalPlayer

	if LocalPlayer5 and LocalPlayer5:IsA("Player") then
		LocalPlayer5:Kick("\nKey Mismatch\n \nIf you aren't a skid trying to bypass MikeyHub, sorry to inform you that theres no direct fix for this.")
	end

	return
end

local LocalPlayer6 = game.Players.LocalPlayer

if LocalPlayer6 and LocalPlayer6:IsA("Player") then
	LocalPlayer6:Kick("Open MikeyHub Loader Before Running This.")
end
